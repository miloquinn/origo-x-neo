# Origo X 代码规范

本规范适用于 Origo X 的 Dart、Flutter、平台壳层、脚本、测试和文档代码。
它的目标只有三个：代码清爽，结构清楚，问题从根本解决。

规范中的“必须”是合并前的硬约束；“应当”只有在有明确技术理由时才能偏离。
任何偏离都要在代码评审中说明原因、影响范围和后续处理方式。

## 一、总原则

### 1. 永远保持清爽

- 修改代码时，顺手删除已经没有调用方的旧代码、重复实现、失效注释、临时日志、无效配置、废弃资源和不再需要的依赖。
- 不保留“以后可能有用”的死代码。需要保留的历史内容放在版本控制或设计记录中，不放在运行时代码里。
- 不能用注释掉的代码代替删除，也不能用 `TODO` 掩盖一个已经知道但不处理的问题。`TODO` 必须包含责任边界和可追踪的后续事项。
- 发现低效架构时，应当修复责任边界、数据流或接口，而不是继续增加缓存、条件分支和重试来掩盖问题。
- 新代码不得与旧路径并存而没有明确迁移计划。迁移完成后，旧路径、兼容分支和迁移专用开关必须删除。

### 2. 永远保持清楚

- 一个文件、类、方法只承担一个可以说清楚的职责。
- 目录按业务域和技术边界分类，不按“临时能放下”分类。
- 一个概念只保留一个权威名称、一个数据模型和一个事实来源；展示模型、传输模型和持久化模型需要转换时，必须显式转换。
- 依赖方向必须清楚：页面组合服务，服务编排领域能力，平台代码提供平台实现；底层不能反向依赖页面。
- 公共接口保持小而稳定。内部实现不为了方便调用而扩大可见性。

### 3. 永远从根因修复

遇到缺陷、性能问题或不一致时，必须按以下顺序处理：

1. 先复现问题，记录输入、状态、调用链和实际结果。
2. 找到违反的契约、错误的数据所有者或错误的生命周期，而不是先加条件判断。
3. 修复共享边界，让所有调用方经过同一条正确路径。
4. 增加能锁定该行为的回归测试。
5. 删除造成重复、分叉或绕过新边界的旧实现。

以下做法默认不接受：只在某个页面加特判、复制一份状态绕开原状态、无限增加重试、吞掉异常、把超时调大、用缓存掩盖错误的数据源。

## 二、命名规范

命名必须表达业务含义、生命周期或数据边界。不要用“能看懂就行”的模糊名称。

### 1. Dart 与文件名

| 对象 | 规则 | 示例 |
| --- | --- | --- |
| 文件和目录 | `snake_case`，按职责命名 | `book_import_service.dart` |
| 类、枚举、扩展 | `PascalCase` | `BookImportResult` |
| 方法、变量、参数 | `lowerCamelCase` | `loadBookMetadata` |
| 私有成员 | 在 Dart 约定上加 `_` | `_cacheRevision` |
| 常量 | 使用有含义的 `lowerCamelCase` | `defaultPageSize` |
| 布尔值 | 使用 `is`、`has`、`can`、`should`、`needs` 等前缀 | `isConfigured` |
| 测试文件 | 与被测对象对应并以 `_test.dart` 结尾 | `book_import_service_test.dart` |

- 文件名必须能说明文件职责；禁止 `misc.dart`、`common.dart`、`helper.dart`、`manager.dart` 作为无边界的垃圾桶。
- `Manager`、`Helper`、`Util`、`Data`、`Info` 只有在它们是稳定领域概念时才能使用；不能用来掩盖职责不清。
- 异步方法用动词表达动作，并返回明确的 `Future`、流或结果类型；不要用 `doSomethingAsync` 作为唯一语义。
- 集合使用复数或明确的集合名，单项使用单数；不要用 `list`、`map`、`obj`、`tmp`、`data2` 等无意义名称。
- 缩写只使用项目、协议或平台的公认缩写；首次出现的领域缩写应在文档或注释中解释。
- 名称一旦成为公共 API、持久化字段或协议字段，不得为了“好看”随意改名；必须评估兼容性并提供迁移。

### 2. 类型后缀必须有意义

以下后缀只在对象确实承担对应职责时使用：

- `Page`：路由页面；`Widget` 不作为单组件文件的强制后缀。
- `Controller`：拥有交互生命周期并协调页面状态。
- `Service`：跨调用方提供领域或应用服务。
- `Repository`：负责领域数据的读写抽象。
- `Store`：拥有并维护一组持久化或运行时状态。
- `Adapter`：把一个外部协议转换为内部契约。
- `Parser`：只负责解析，不顺便保存、请求网络或修改页面状态。
- `Result`、`Error`、`Exception`：明确表达返回结果和失败边界。

如果一个类同时符合多个职责，应先拆分职责，而不是堆叠后缀。

## 三、分类与目录

- `lib/core/` 放跨页面共享的核心模型和纯能力；不得放某个页面的临时状态。
- `lib/pages/` 放页面装配和交互；复杂页面拆到所属功能域的 `parts/`，状态所有权仍由主页面或控制器明确持有。
- `lib/services/` 放应用服务、持久化、同步、网络和平台协调；不要把页面绘制逻辑塞入服务。
- `lib/widgets/` 只放真正跨功能域复用的 UI；只被一个页面使用的组件留在该页面域内。
- `lib/book_sources/`、`lib/reader_core/` 等领域目录保持独立契约；跨域引用通过公开接口，不直接读取对方内部状态。
- `android/`、`ios/`、`windows/`、`macos/`、`linux/`、`web/` 只承载平台启动、窗口、系统 UI 和插件注册；业务逻辑优先留在 `lib/`。
- 生成文件由对应工具维护，不能手工加入业务逻辑；源文件改变后重新生成并校验差异。
- 同一事实只能有一个来源。配置、枚举、格式注册表、路由 ID、文案键和协议字段禁止在多个目录复制维护。

新增目录或跨域依赖前，先回答：它属于哪个责任边界、谁拥有状态、谁负责失败、为什么现有目录不能承载。如果说不清楚，不得新增。

## 四、架构与实现

### 1. 依赖方向

- 页面只负责展示状态、接收输入和发起动作；业务规则放在可测试的服务或领域对象中。
- 服务不依赖具体页面；平台实现通过接口或明确的桥接层注入。
- 纯逻辑优先保持无副作用；文件、数据库、网络、平台通道等副作用集中在边界层。
- 不使用隐式全局可变状态。单例或静态状态必须有明确的所有者、初始化时机、清理时机和并发策略。
- 新增抽象必须解决真实的边界或替换需求；不能为了“看起来更架构化”增加空接口、转发类和包装层。

### 2. 数据与状态

- 明确区分领域模型、传输模型、存储模型和展示模型，禁止让一个模型承担所有层的偶然字段。
- 状态只能有一个写入者或一个协调者；其他模块通过命令、事件或只读接口访问。
- 缓存必须说明键、有效期、失效条件、容量和所有者；没有失效策略的缓存不应加入。
- 文件和数据库写入应当可恢复、可校验、可原子替换；迁移必须考虑旧数据、失败回滚和重复执行。
- 不把整本书、大文件或无限集合一次性读入内存；使用流式处理、有界批次和明确的并发上限。
- 重试必须有次数、退避、取消和最终失败结果；不得让重试掩盖确定性错误。

### 3. 错误处理

- 异常必须在能做出正确决策的边界处理；不能无条件 `catch` 后忽略。
- 面向用户的错误要说明可行动的原因；日志保留足够的上下文，但不得泄露密钥、令牌、个人数据或完整敏感内容。
- 可恢复错误、用户输入错误、权限错误、协议不兼容和数据损坏必须能区分，不能统一返回“失败”。
- 失败路径必须和成功路径一样被设计、测试和验证；不能只测试按钮能否点击。

## 五、清理与重构流程

### 小改动

1. 搜索调用方、配置、测试和文档引用。
2. 修改共享契约或唯一实现。
3. 删除失效分支、重复代码和不再使用的资源。
4. 运行格式化、静态分析和相关回归测试。

### 大重构

1. 写清现状、目标边界、迁移顺序和回滚方式。
2. 先补足缺失的行为测试，锁定现有正确行为。
3. 按责任边界逐步迁移，不同时引入多个新抽象。
4. 每完成一段就删除旧路径并搜索残留引用。
5. 完成后检查依赖、文档、平台桥接、构建脚本和资源是否还有旧名称或旧流程。

重构完成的标准是：新边界成为唯一入口，旧实现已删除，调用方不再需要兼容分支，测试能证明行为没有悄悄改变。

## 六、注释、文档与 TODO

- 注释解释“为什么这样做”、外部限制、不变量和边界；代码已经清楚表达的“做了什么”不重复注释。
- 不在注释里保留已删除方案的历史叙述；历史决策写入设计记录或提交说明。
- 性能结论必须有测量依据；没有数据时只能描述假设，不能把猜测写成事实。
- 修改公共契约、目录边界、数据格式、发布流程时，同时更新对应文档。
- `TODO` 必须写清问题、责任人或责任模块、触发条件和处理期限；过期 TODO 要删除或升级为明确任务。

## 七、测试与验证

- 业务逻辑变化必须有针对触发条件和失败路径的回归测试；不要只依赖快照或“页面能打开”。
- 共享状态、平台通道、图片编解码和单例会污染进程的测试，应隔离进程运行，不能靠跳过断言掩盖测试基础设施问题。
- 先运行最小相关测试，再运行完整测试；测试失败时先判断是产品、测试隔离、环境还是构建层问题。
- 合并前至少执行与改动匹配的格式化、分析、测试和构建检查。仓库基础命令见 [CONTRIBUTING.md](CONTRIBUTING.md)。
- 平台构建、核心回归测试和有状态 Widget 测试是不同信号，报告时必须分别说明，不能把其中一层的失败扩大成另一层的结论。

## 八、代码评审清单

评审者和提交者都必须确认：

- [ ] 这次改动是否修复了共享根因，而不是增加局部特判？
- [ ] 是否删除了旧实现、死代码、重复模型、失效文档和无用依赖？
- [ ] 文件、目录、类、方法和变量是否能直接表达职责？
- [ ] 是否只有一个事实来源和一个状态所有者？
- [ ] 是否存在无界读取、无界并发、无界重试或重复 I/O？
- [ ] 错误、取消、超时、权限和数据损坏路径是否有明确行为？
- [ ] 公共 API、持久化数据和协议变更是否考虑兼容与迁移？
- [ ] 是否补充了能防止回归的测试，并实际读取了测试结果？
- [ ] 文档、生成文件、平台桥接、资源和构建脚本是否同步？

## 九、偏离规范

临时兼容、平台限制或性能实验可以偏离规范，但必须同时记录：

- 偏离的具体位置和原因；
- 影响的模块、数据和用户行为；
- 删除或收敛它的条件；
- 对应测试和负责人。

没有边界、没有退出条件的“临时方案”不得进入主分支。

## 十、完成定义

一项代码改动只有在以下条件都满足时才算完成：

1. 行为和责任边界清楚，命名与分类一致。
2. 根因已修复，旧路径和冗余实现已清理。
3. 失败、取消、迁移和性能边界已处理。
4. 相关测试、格式化、静态分析和构建验证已通过，或已明确记录实际阻塞层。
5. 文档、生成代码、平台代码和资源没有遗留旧契约。

