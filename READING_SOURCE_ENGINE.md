# 阅读书源执行引擎

更新日期：2026-09-09

本轮补齐 DOM 索引、显式 CSS 模式、正文模板及脚本取值/隔离语义，新增可复现的全量语料审计。
详见 [兼容性验证记录](docs/source-compatibility-2026-09-09.md)。7,506 个唯一配置的结构准备率为
91.46%；80% 的完整执行兼容目标仍需配对验证，不能以结构率或单端抽测代替。

## 目标与边界

Origo X 原生解析、保存和执行通用阅读书源 JSON。导入只做本地结构解析、按
规范化 `bookSourceUrl` 去重和能力摘要，不逐条访问网站，也不以短时联网结果删除书源。
站点失效、反爬限制和规则过期应在用户实际使用对应能力时呈现，不能被误报为导入失败。

文字书源目前可以按需执行搜索、发现、详情、目录和正文链路。图片、音频、视频与文件型
来源不会冒充文字来源。登录、购买、验证码和任意外部副作用仍需要独立的交互与安全设计。

## 已实现

- 单对象、数组、包装对象和嵌套 URL 清单导入；64 MiB、最多 10,000 条边界。
- 大型 JSON 在后台 isolate 解析；注册表批量序列化和保存一次。
- 导入前提供精确、标准和同站审查三种去重模式。标准模式规范化协议/主机大小写、默认
  端口、尾斜杠和 query 顺序，移除常见追踪参数，同时保留 `#简体`、emoji 等身份标签。
- 重复审查保留每份完整配置，按兼容能力、规则完整度、启用状态和更新时间给出可解释的
  推荐项；用户可逐项改选，切换模式不会重新下载输入。同站结果仅作为疑似项，默认全保留。
- 书源管理页可单独扫描已安装的 Reading Source 重复项；确定重复默认关闭非推荐项，不做
  永久删除或来源 ID 迁移。ORSP 不参与 Reading Source 的跨协议合并。
- “书源整理”统一承载联网体检与本地重复项整理。体检进度可收起并在应用进程内静默继续，
  返回管理页后可恢复查看进度或审查结果；系统终止应用后不承诺继续执行。
- 普通选择器、CSS、常用 XPath、RFC JSONPath、正则链、`@put` / `@get` 状态规则。
  旧式 DOM 规则兼容 `&&` 合并、`||` 回退、`%%` 按位置交错合并、方括号索引/排除/
  区间/倒序，以及 `@all`、`@ownText`、`@textNodes` 等终值语义。
- 搜索/发现阶段产生的书籍变量随书籍元数据传到详情、目录和正文，并在加入书架后继续保留；旧书架快照缺少变量时，会对常见“详情 URL 模板 + `@put`”组合做无网络反推，跨页面规则不再因状态清空生成缺段 URL。
- GET、POST、UTF-8、GBK/GB2312/GB18030、请求级与来源级请求头。
- 请求模板分页算术、第一页/后续页候选、协议相对 URL、重定向最终 URL；章节 URL 绝对化时保留末尾请求选项，POST、请求头与后台网页加载配置不会被编码进真实地址。
- 每来源独立 Cookie 会话、浏览器语义重定向、取消、响应大小限制。
- QuickJS 脚本、同步网络调用重放、常用摘要/加密/Base64/DOM 辅助方法。脚本侧
  `source.get` / `source.put` 按来源隔离保存键值，来源名称与类型可读；`getStringList` 和
  `getElements` 返回值兼容 Java 集合的 `size()`、`get()`、`isEmpty()`、`toArray()`。
- 脚本 Cookie API 与现有每来源 Cookie jar 共用状态，支持 `getCookie`、按键读取、设置和
  清理；未启用 `enabledCookieJar` 的来源不会获得跨请求持久 Cookie。
- 脚本同步请求响应对象暴露正文、最终 URL、状态码、响应 headers 和响应 cookies；摘要
  工具同时提供完整 MD5 与常见的 16 位截取形式。
- 脚本提供 `java.t2s`、`java.s2t` 以及对应全局繁简转换函数，覆盖来源分类标签和正文
  预处理的常见调用。
- 请求模板和脚本网络桥支持 `HEAD`，响应正文为空但状态码、最终 URL、headers 与 cookies
  正常返回。
- 脚本 `cache` 支持按来源隔离的 `put/get/delete`、内存别名和有效期；内联 `jsLib` 在每次
  规则执行前加载，使公共函数可被 URL 与解析规则复用。远程脚本清单仍未绕过受控网络层。
- 登录态支持静态/脚本 `loginUi` 表单、系统安全存储中的每来源登录信息与登录 Header、
  登录 Header 自动注入请求、脚本读写接口，以及搜索、发现、详情、目录、正文响应后的
  `loginCheckJs`。
- Android 支持脚本发起可见浏览器和图片验证码交互。`startBrowser`、`startBrowserAwait`
  与验证码读取会暂停当前异步规则任务，等待用户完成、取消或超时；完成后把最终 URL、
  DOM、Cookie 或验证码值注入确定性脚本重放。内嵌 HTML、验证后重新请求、每来源排队和
  Cookie 回写均走现有安全边界。其他平台明确返回暂不支持，不会退化为静默后台页面。
- DOM 集合兼容 `select/text/html/outerHtml/attr/first/last/remove`，并继续提供 Java 集合接口。
- Android 后台网页加载、页面脚本、最终 DOM/URL 与 Cookie 回传。
- 正文规则命中多个节点时按换行合并，再执行清理表达式；普通点号默认不跨段落吞掉后续正文，明确使用跨行写法的规则仍可覆盖多段。
- 旧式与 JSON 发现频道、动态发现脚本、频道分页书单。
- 简介中的 HTML、字面转义换行、Unicode 转义、实体和不可见控制字符清洗。
- 发现页标准/列表双布局，以及大型书源管理页的搜索、筛选与批量操作。
- 书籍源与漫画源共用同一个导入解析器，因为社区聚合 JSON 经常混装两类来源；导入预览会分别显示书籍源、漫画源和当前不可运行的数量。显式 `bookSourceType: 2` 优先，旧源错误写成 `0` 时，仅在目录/正文规则完整且正文具有图片特征时保守推断为漫画源。
- 只有名称、地址、启用状态等字段而没有搜索/目录/正文规则的“配置残片”会显示为当前不可运行，不会伪装成已适配的漫画源。

## 真实聚合文件基线

本地测试文件不提交到仓库：

- `shuyuan.json`：解析 1,048 条，无效 6 条，文件内重复 7 条。
- `shareBookSource.json`：解析 3,880 条，无效 57 条，文件内重复 12 条。
- `shareBookSource.json` 中 3,050 条为完整本地能力、332 条需要后台网页能力、498 条缺少文字阅读核心规则或属于其他内容类型。
- `shuyuan.json` 中 894 条为完整本地能力、90 条需要后台网页能力、64 条缺少文字阅读核心规则或属于其他内容类型。
- 两份合并后 4,561 个唯一地址，其中 4,033 个具备可尝试的文字阅读链路，约占 88.4%；该数字不是站点在线率或完整阅读链通过率。

2026-08-03 分层真实链抽测覆盖普通选择器、脚本、XPath 和后台网页四类。12 个跨站样本中，
1 个 XPath 样本完整通过搜索→详情→目录→正文，得到 1,826 章目录和 2,299 字正文；其余
主要落在站点 403、规则已过期、目标站不可连接和来源脚本自身空值异常。另一个真实来源
已从目录地址缺少书籍变量导致的 404 推进到取得 666 章目录和完整正文 DOM；正文多节点
合并与清理语义现有对应回归。真实抽测结果用于定位引擎缺口，不用于导入过滤。

## 安全与兼容策略

- 每次请求前解析并检查目标地址，默认拒绝私网、回环、链路本地和非法协议。
  用户在设置中开启「允许内网书源」后，可访问本机和局域网目标；未指定、组播和
  非法协议仍然拒绝。
- 普通公网 DNS 使用固定地址连接，降低 DNS 重绑定风险。
- 检测到虚拟 DNS 使用的 `198.18.0.0/15` 保留地址时，在完成相同地址检查后使用系统网络
  通道，避免自定义连接通道破坏本地隧道并把有效响应变成 HTTP 400。
- 普通固定地址通道若对幂等 GET/HEAD 收到 HTTP 400，会在相同目标地址安全校验后通过
  系统网络通道重试一次，以兼容 VPN/代理接管；POST 不重放，避免重复提交。
- WebView 请求和最终跳转地址都执行网络策略校验。
- 脚本网络调用暂停后交由 APP 网络层执行，再把正文和最终 URL 注入脚本重放；不同来源
  的脚本上下文与变量互不覆盖。
- 非 HTTP(S) 的合法 `bookSourceUrl` 作为不透明身份处理，只做去空白后的精确比较；同站
  审查不会把它们归入域名组。规范化等价来源重新导入时保留原有 source ID。

## 仍需继续覆盖

- iOS、macOS、Windows、Linux 和 Web 的可见浏览器验证宿主；Android 已接入。
- WebView 响应状态码/headers 的完整回传。
- JSON 形式的远程共享脚本清单及其受控缓存。
- 简繁转换、少量 RSA/Java 类导入和 Java 风格 DOM API。
- 脚本执行的硬超时与内存上限。
- Android 真机上的后台网页批量回归，以及更多真实来源的分层长期抽测。
- 需要把浏览器验证、登录态、付费章节和站点特有风控纳入可重复的 Android 真机样本矩阵；当前仍不以导入成功等同于完整阅读链可用。

在真实来源全链覆盖达到可复现的统计标准前，不对外宣称固定百分比兼容率。

## 阅读书源兼容实验室

`tool/reading_source_lab/` 是独立的离线解析与兼容审计项目。它读取对象、数组、常见包装
对象和 `sourceUrls` 清单，按书源地址去重，统计规则能力依赖，并生成 JSON/Markdown
兼容矩阵。工具不联网、不执行书源脚本，也不输出 header、登录或 Cookie 配置值。

三批样本按文字类型、有效地址、搜索、目录和正文五项必要结构统计，核心阅读链结构覆盖为
3,547 / 4,139（85.7%）。该比例是离线可尝试基线，不等于真实站点成功率；登录、网页
交互和高级脚本 API 继续作为扩展能力单独统计。

2026-08-04 使用用户提供的 141、62、3,949 条三批样本生成了新的本地基线：去重并跳过
结构错误后共 4,139 条可审计配置，主要部分兼容来源集中在 JavaScript、登录态、WebView、
Java DOM 辅助 API 和 XPath 子集。聚合报告位于
`tool/reading_source_lab/docs/sample-baseline.md`；该报告只描述结构兼容，不代表网站在线率。
