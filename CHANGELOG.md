# Open Reading 更新日志

本文件记录 Open Reading 各公开版本的主要变化。版本按发布时间倒序排列。

## v2.7.1 - 2026-09-22

- 统一适配 Legado 书源通用规则，兼容旧式请求头、浏览器交互和可选会话存储，改善来源导入与搜索、目录、正文阅读链路。

版本：`2.7.1`，基础构建号：`260916004`。

- 修复使用 Newsreader 阅读字体阅读 TXT 时中文 fallback 在不同页面间切换衬线与无衬线字体的问题，统一使用衬线 fallback。
- 英文产品名称统一更新为 **Origo**，覆盖应用标题、设置页、系统显示名称和 App Store 英文元数据。
- 继续保留既有包名、服务地址、WebDAV 路径与 Open Reading Source Protocol 名称，已有安装、同步与书源配置无需迁移。
- 同步优化英文、日文及繁体中文界面的品牌文案与辅助功能标签。

## v2.7.0 - 2026-09-12

版本：`2.7.0`，基础构建号：`260912001`。

- 统一更多菜单的样式与开合动画，卡片内保留普通三点入口；重排书源卡片，改善长名称、分组和大字号显示。
- 新增书源编辑与独立书籍详情页面，完善书源更换和重新匹配流程。
- 初步支持书源登录：可通过书源自定义表单或原网站完成登录，安全保存、恢复和清除 Cookie 与 Local Storage，后续书源请求可继续使用登录状态。
- 书源整理支持暂停、继续、结果筛选，以及批量停用或删除已检测书源。
- 新增书源书籍更新页面，可选择章节范围，查看版本差异并处理正文冲突。
- 优化听书播放器和设置布局，完善音量、音色、语速、定时与跨章节朗读，跳过空白朗读文本。
- AI 模型和云端 TTS 使用独立配置页面，支持自定义兼容服务、模型与音色。
- 新增应用界面字号设置，改善窄屏、平板和放大字号下的显示。
- 调整阅读位置恢复与保存，改善 EPUB 分页、连续滚动和章节切换后的定位。
- 更新书籍文件与正文同步流程，完善版本校验、冲突处理和同步状态展示。
- 统一书籍本地存储路径，补充旧路径迁移、文件修复与书籍身份关联。
- 修复 iOS 上书库偶发缺失封面的问题。
- 音量键翻页默认关闭，避免误触。

## v2.6.7+260911001 - 2026-09-11

版本号保持 `2.6.7`，基础构建号更新为 `260911001`。

- 发现页会先展示已加载完成的书源，刷新时保留现有内容。
- 漫画连续阅读时，图片加载不再把正在看的位置顶走。
- 漫画目录改为阅读主题样式，打开后定位到当前章节。
- 阅读搜索跳转更准确；横滑翻页在滑动结束后再确认进度。
- 网络异常时不再误退出登录，账号页可以直接重试。
- 书源开关和收藏保存失败时会提示，并恢复原来的状态。

## v2.6.7+260910002 - 2026-09-10

版本号保持 `2.6.7`，基础构建号更新为 `260910002`。

- 统一高级会员页与设置页会员卡风格，优化会员状态、权益、购买和恢复购买的层次，适配手机、平板及深浅主题。
- 移除会员页退款快捷入口，保留恢复购买、会员条款和隐私说明。
- 将内置隐私说明中的下载明细保留期限更正为实际的 180 天。

## v2.6.7+260910001 - 2026-09-10

版本号保持 `2.6.7`，基础构建号更新为 `260910001`。

- 修复 iOS 批量书源体检的脚本引擎回调隔离与原生资源释放，降低闪退风险，完善超时和迟到回调处理。
- 完善书源 JSONPath 模板、脚本插值、Java 辅助方法和特殊 HTML 属性兼容，修复小说源被误识别为漫画源的问题。
- 优化目录抽样、多页正文与阅读器搜索导航；删除全部书源时同步清理分组，改善代理 IPv6 Fake-IP 网络兼容。
- 更新 Premium 页面和 Apple 购买验证、恢复及退款申请流程，补充应用内会员与隐私说明。
- 高级书源协议和局域网书源访问要求已验证的 Premium 权限；普通阅读、ORSP、WebDAV 和 AI 设置保持可用。
- 优化平板顶部渐变模糊计算与重建开销，桌面宽窗口复用顶部悬浮导航；整理关于页面、社区入口与品牌图标。

## v2.6.7 - 2026-09-07

- 重新设计平板页面，优化横竖屏、分屏和大字号显示。
- 平板新增悬浮导航与顶部渐变模糊，浏览更清爽。
- 发现页新增平板侧栏，书源选择、收藏、分组和搜索更方便。

## v2.6.5 - 2026-09-05

版本：`2.6.5`，基础构建号：`260905001`。

- 新增自动翻页，支持定时、扫屏、匀速和间隔滚动，可调速度、暂停与继续。
- 新增 TXT 本章编辑，支持保存正文修改和恢复历史版本。
- TXT 正文支持双向同步、版本对照和冲突处理，可选增量同步以减少传输。
- 跨设备续读更可靠：开书检查最新进度，阅读中提示更新，并可返回原位置。
- 重做云端同步页面，完善自动同步与失败重试，分开展示进度和正文状态。
- 优化大体积 TXT 导入、打开与编辑，完善本地与在线阅读缓存，减少内存占用和重复加载。
- 书源新增收藏、分组排序和批量管理，发现页可按收藏、分组或单个书源筛选。
- 完善链接与 JSON 文件导入，支持预览、重复项选择、进度提示和取消，大合集导入更流畅。
- 书源体检支持中断续查、失败重试和结果筛选，整理时默认保留书架正在使用的书源。
- 提升在线书籍与漫画兼容性，修复目录缺章、图片缺失、链接错误及简介消失等问题。
- 焕新书源列表、推荐分类和阅读控制栏，优化切换动画，修复返回分类后书单不显示。
- 完善 iOS 隐私说明与上架准备，增强 Android 安装包校验。

## v2.6.4 - 2026-09-05

版本：`2.6.4`，基础构建号：`260904001`。

### WebDAV 阅读数据同步

- **笔记与高亮可选择同步**：同步范围新增笔记、高亮、下划线与手绘标注开关，默认保持关闭，并明确提示 WebDAV 元数据未端到端加密；新增和旧版标注身份都能稳定兼容，删除也会跨设备传播。
- **阅读体验设置完整同步**：字号、亮度、字重、行距、字距、对齐、页边距、主题、翻页模式、段落样式、点击区域、TXT 章节页与图片背景可按字段合并；自定义主题会同步颜色和排序，但本机背景图片路径不会上传。
- **替换规则可选择同步**：新增默认关闭的替换规则范围，可同步表达式、替换文本、适用范围、启用状态和顺序；远端更新会立即刷新当前阅读进程。
- **同步范围严格生效**：关闭某项范围后，本地扫描、远端应用、待上传计数和新批次发布都会同时停止，尚未上传的敏感数据不会继续进入后续批次。
- **书源秘密留在本机**：公开 ORSP 书源和安全书籍快照使用严格字段白名单；兼容书源的可执行配置、请求头、登录信息、封面请求头、运行变量，以及含查询参数或凭据的书源身份不会上传。旧版本已经写入远端不可变历史的秘密无法自动抹除，建议相关用户轮换令牌并更换或清空同步目录。
- **跨版本合并更可靠**：未来版本的未知阅读设置会保留待应用状态；旧版标注别名统一按最新逻辑时钟合并，避免删除后复活或旧内容覆盖新内容。
- **失败恢复与损坏保护**：远端记录先持久化再幂等应用，中断、范围关闭或依赖暂不可用时可在下次同步继续恢复；本地主题或替换规则缓存损坏时会保留原始数据和远端备份，避免误发批量删除。

### 桌面端阅读交互

- **键盘翻页更完整**：文字书与在线书源阅读器支持方向键、Page Up、Page Down、空格和 Shift+空格前后翻页；组合键会留给系统或当前控件处理。
- **鼠标与触控板翻页**：分页模式支持鼠标滚轮和触控板手势，并限制一次连续手势只翻一页；纵向阅读仍保留原生连续滚动。
- **主题列表更易操作**：阅读设置中的横向主题列表支持鼠标拖动和垂直滚轮滚动，同时保留漫画阅读器所需的紧凑主题卡片配置。

## v2.6.3 - 2026-09-04

版本：`2.6.3`，基础构建号：`260901001`。

### 漫画与图片阅读

- **本地与在线漫画统一阅读**：本地 CBZ 和在线漫画共用同一个阅读器、章节进度、失败重试、翻页方向和缓存管理，旧的重复阅读页面与控制栏已经清理。
- **向上回看不再抖动**：修复上下连续滚动中手指向下滑时页面反复拉扯、抖动或无法回到上一页的问题，图片延迟解码后也会保持稳定位置。
- **控制栏与进度条重新设计**：顶部和底部只保留一套控制样式；底部使用更清晰的粗进度轨道、更大的拖动点和紧凑操作区，不再显示臃肿的大椭圆。
- **阅读设置与书籍一致**：漫画设置改用书籍阅读器同款界面，提供跟随系统、浅色模式和深色模式，并统一管理上下连续、从左到右和从右到左三种阅读方向。
- **主题跟随和偏好持久化**：阅读主题与方向会自动保存；跟随系统模式会在系统亮暗变化时同步更新漫画背景、控制栏和进度条。

### Android 漫画导入

- **CBZ 可直接打开和分享导入**：Android 支持通过“打开方式”、分享和多文件分享接收 CBZ，并识别常见漫画压缩包类型。
- **导入校验更可靠**：导入前确认压缩包包含可阅读图片，拒绝损坏文件和伪装格式；应用私有副本在升级后仍可正常打开。

### 性能与内存

- **长漫画只保留附近内容**：连续阅读限制章节与图片数据窗口，切换章节、退出阅读和系统内存紧张时及时释放旧页面和可重建缓存。
- **图片内存占用更低**：限制预加载和解码缓存，及时关闭压缩包文件句柄；EPUB 与 Kindle 图片直接共享字节，移除 Base64 膨胀和重复解码。
- **系统内存压力统一处理**：内存不足时清理正文、封面、头像、分页和 Flutter 图片缓存，同时保留当前阅读内容与磁盘数据。
- **大型书籍不再长期滞留**：大型内存文档绕过重开缓存，仍保留磁盘分页和索引能力，降低长时间阅读后的内存增长。

### 结构清理与稳定性

- **删除旧阅读链路**：移除被统一漫画阅读器替代的旧页面、重复控制组件和失效服务，减少本地与在线行为再次分叉。
- **补充关键回归测试**：覆盖 CBZ 导入、章节缓存窗口、主题持久化、方向切换、图片延迟加载和拖拽定位稳定性。

## v2.6.2 - 2026-08-24

版本：`2.6.2`，基础构建号：`260824001`。

> **说明：漫画阅读功能仍在进一步完善中。** 本版本修复了多项图片解析、连续阅读和模式切换问题，但不同站点的页面结构与图片加载策略差异较大，后续版本还会继续提升兼容性与稳定性。

### 在线书源与缓存

- **发现页加载更快**：推荐、分类与分类浏览统一使用可持久化缓存，覆盖 ORSP 和兼容文字/漫画书源；重复打开时优先展示已有结果，并按书源配置版本隔离数据。
- **缓存更新更准确**：登录、退出登录或刷新书源后会同步失效相关发现缓存；HTTP 请求支持 ETag 与 Last-Modified 条件重验证，内容未变化时复用本地数据，临时故障时只在限定时间内安全回退。
- **并发和失效更可靠**：合并同时发生的刷新请求，限制内存与磁盘缓存容量，并防止已经清理或更新的缓存被后台旧请求重新覆盖。

### 漫画与图片阅读

- **多图片规则不再只取第一张**：支持一次规则命中的多张图片地址，并为更多常见漫画容器和旧类型书源提供安全兼容，减少章节图片缺失或无法识别的问题。
- **纵向连续阅读更稳定**：优化章节衔接、拖拽定位、空章节进度和图片加载后高度变化处理，降低跨章节跳动、进度错位与当前位置被拉回的问题。
- **阅读模式切换更清晰**：完善纵向阅读设置面板和横向模式切换，避免异步设置读取或旧阅读源回调覆盖当前状态。
- **漫画仍在持续完善**：后续将继续针对更多站点、长章节和复杂网络环境优化兼容性、内存占用与交互细节。

### 替换规则与阅读稳定性

- **复杂规则不再阻塞界面**：正则替换改为异步批处理，并在可终止的隔离执行环境中运行，避免复杂或异常规则长时间卡住阅读界面。
- **异常规则安全降级**：新增替换超时、输出膨胀、无效正则和工作进程异常保护；规则意外清空正文时会保留原文，并记录诊断信息。
- **排版缓存跟随规则更新**：章节标题与正文会在进入排版前批量完成替换，分页缓存按规则版本隔离，减少规则变更后继续复用旧页、标题闪变和打开卡顿。

### 跨平台阅读排版

- **默认阅读字体保持一致**：TXT、EPUB 与在线书源统一解析平台默认阅读字体，使测量、分页、正文绘制、设置预览和缓存身份使用同一套字体回退链。
- **保留用户与书籍字体优先级**：Android 默认使用无衬线字体，Apple 平台优先使用苹方，Windows 优先使用微软雅黑；用户明确选择的字体和 EPUB 内嵌字体优先级不变。

## v2.6.1 - 2026-08-20

版本：`2.6.1`，基础构建号：`260820001`。

### 在线书架与书源兼容

- **修复在线书架打开异常**：首页、书库和启动恢复现在使用同一条在线书籍解析链路，在进入阅读器前校验书源配置、书籍快照和身份信息；损坏、缺失或错配的数据不会再通过空值强制解包进入不稳定状态。
- **更多兼容书源可正常浏览和翻页**：部分书源把封面、目录下一页或正文下一页等可选字段返回为非网络地址时会安全忽略，不再让整本书加载失败；结果条目缺少有效 `bookUrl` 时会尝试使用真实书籍链接。
- **脚本正文片段不再遗漏**：DOM 单值规则命中多个内容节点时按正文顺序合并，减少只保留第一段导致的内容缺失。
- **漫画章节缓存保留图片信息**：目录和章节缓存会一并保存远程图片地址与请求头，重复打开缓存章节时仍能恢复完整图片页。

### 漫画与图片阅读

- **新增上下连续滚动**：图片阅读新增纵向连续模式，并保留从左到右、从右到左两种整页翻阅；阅读方式按书保存，在线漫画默认使用纵向阅读。
- **跨章节连续阅读更稳定**：目录跳转、当前页进度和跨章节浏览保持同步，远离当前阅读位置的章节内容会及时回收，降低长时间阅读的内存占用。
- **图片失败可原地重试**：网络请求或图片解码失败后可直接在当前页重试，重试前会驱逐对应损坏缓存，无需退出阅读器。
- **首屏图片优先加载**：可见图片请求优先于后台预加载；正文图片使用独立的长效缓存与更合适的容量限制，减少首屏等待并避免与书源封面缓存相互挤占。
- **统一图片阅读界面**：漫画、PDF 和书源图片章节统一使用主题化工具栏、跳页操作和阅读设置。

### 大型 TXT 导入

- **元数据分析不再扫描完整大文件**：大型 TXT 只读取文件头并在后台 isolate 中分析书名、作者和编码，减少导入时的内存占用和主线程等待。
- **书名识别更准确**：没有明确书名或作者标记时优先使用文件名作为书名，不再把正文第一行或数字编号误识别成标题；明确标注的书名和作者仍会保留。
- **多兆字节文本更快打开**：大型 TXT 的分段读取阈值提前到 2 MiB；没有标准章节标题的超长文本也会切成有界区段，避免单个巨大章节拖慢分页。

### 缓存与稳定性

- **缓存管理覆盖正文图片**：缓存统计和清理现在同时包含书源封面与正文图片页缓存。
- **补充关键回归测试**：覆盖在线书架数据校验、书源 URL 安全降级、图片缓存恢复、可见请求优先级、纵向漫画进度、失败页重试和大型 TXT 分段。

## v2.6.0 - 2026-08-17

版本：`2.6.0`，基础构建号：`260817001`。

### 后台听书与系统媒体控制

- **朗读会话持续运行**：听书状态提升为 App 级会话，离开当前阅读页或切换到其他应用后仍可保持正在播放的书籍、章节和句子位置；切换书籍时会安全结束旧会话。
- **减少句间停顿并改进高亮**：系统 TTS 可批量排队相邻句子，连续朗读更自然；正文高亮、暂停恢复和进度保存跟随当前真实句子位置，Android 语速调整也更稳定。
- **完善 iOS 锁屏体验**：iPhone 和 iPad 使用 spoken-audio 音频会话，锁屏与控制中心显示当前书名、章节和进度，并支持播放、暂停、上一句、下一句与停止；电话、耳机等系统中断可以正确暂停和恢复。
- **统一 Android 与 iOS 控制协议**：Android 前台媒体通知、耳机媒体键和 iOS Now Playing 使用同一朗读控制链路，后台控制后的页面状态保持一致。

### 平板仿真翻页

- **双页装订线对齐真实中线**：左右纸张围绕屏幕中央的同一条装订线翻动，24px 中缝作为纸张内部留白保留；右页结束翻动时不再从中缝边缘突然向中央瞬移。
- **真实背页保持完整显示**：平板双页翻起的背面使用相邻页真实内容，保留正常颜色与不透明度，不再叠加手机单页使用的减淡纸面高光。
- **整个双页区域按方向翻页**：无论从左页还是右页起手，向左滑都会由右页跟手翻到下一组，向右滑都会由左页跟手翻回上一组；不再只有最外侧约 30% 能触发。
- **修复相邻页单帧闪烁**：从页面内侧起手时，平铺首帧严格限制在当前纸张范围，避免右页纹理越过装订线导致左页偶尔闪一下。

### 在线书源与发现页

- **目录和正文缓存覆盖更多书源**：兼容阅读源复用本机目录与章节缓存，重复打开在线书籍时优先显示已有内容；旧缓存会在后台刷新，书源变慢或短暂离线时仍可继续阅读。
- **缓存按配置和登录状态隔离**：书源配置、书籍变量和登录状态变更会自动使用新的缓存版本，避免不同账号或配置误用旧目录与正文；整本下载仍会获取最新内容。
- **发现页加载失败可恢复**：本地书源注册表读取或解析失败时不再永久停留在加载状态，而是显示可重试错误，修复数据后可以直接重新加载。

### 账号兼容性

- **邮箱验证码登录兼容未设置用户名的账号**：新账号通过邮箱验证码首次登录时，即使服务端尚未分配用户名，也会使用有效显示名或邮箱前缀安全进入账号页面，不再因为空用户名触发客户端解析错误。

### 隐私与数据保护

- **关闭 Android 普通应用备份**：应用私有数据库、账号状态和阅读数据不再进入系统的常规备份/恢复通道，减少敏感本地数据被复制到非预期设备或备份介质的风险；应用内 WebDAV 等显式同步能力不受影响。

### 架构、清理与性能

- **清理旧朗读与历史状态代码**：移除已经由 App 级听书会话替代的阅读页播放控制、重复显隐状态和失效基础设施，减少旧链路与新媒体会话同时存在造成的状态分叉。
- **拆分核心模块边界**：将 AI 配置、协议适配、模型预设、错误翻译、设置页、主题状态、书籍导入元数据和书源页面等职责拆成聚焦模块，降低大型文件耦合并让后续修改只影响对应功能。
- **减少重复初始化与无效工作**：统一服务和页面状态所有权，在线目录与正文命中缓存时直接复用已有结果，后台刷新、磁盘写入和媒体状态同步不再阻塞当前阅读展示。
- **提升关键路径稳定性**：发现页失败能退出加载状态，听书切换和系统中断有明确恢复路径，平板翻页首帧和页面交接保持稳定，减少重复请求、重复重建和短暂闪烁。
- **补充关键回归测试**：覆盖后台朗读媒体会话、iOS TTS、在线书源缓存、发现页失败恢复、平板双页装订坐标、真实背页颜色、全区域双向手势和相邻页闪烁。

## v2.5.9 - 2026-08-13

版本：`2.5.9`，基础构建号：`260811001`。

### 书源整理

- **新增书源体检**：可联网检查搜索、详情、目录和正文能力；体检可以在 App 运行期间后台继续，完成后集中查看需要处理的书源。
- **新增重复项整理**：导入前和已安装书源均可按精确、标准或同站模式检查重复项，并结合兼容性、能力完整度和健康状态推荐保留项；整理默认只关闭重复书源，不删除配置。
- **完善书源信息入口**：书源管理页新增协议、项目地址和内容权利说明入口，导入分析与批量管理操作也更清晰。

### 账号修复

- **修复浏览器登录回跳误报断网**：GitHub 等浏览器授权返回 App 时，网络连接切换可能让第一次授权查询短暂失败。现在会在授权有效期内静默重试，不再先弹“无法连接账号服务”再于数秒后显示登录成功。
- **登录成功不再被附加资料否定**：会员、邀请和安全状态属于登录后的附加资料；其临时失败不会把已经有效的会话误报为登录失败，明确的授权过期、拒绝和服务端错误仍会正常提示。

### 数据与恢复

- **优化批注查询**：为书籍、定位和记录序号建立联合索引，文本批注保存时会复用最新记录，避免重复写入并提升查询效率。
- **图片失败自动恢复**：头像和书源封面解码失败后会清理对应缓存并自动重试一次，减少缓存损坏导致的持续空白。
- **损坏缓存安全降级**：无效账号摘要和自定义阅读主题会被忽略并清理；更新检查和图片重试失败会保留诊断信息而不影响主流程。

### 发现页

- **列表浏览更顺滑**：发现页列表布局中的书源和分页加载书籍会以轻量弹出动画进入；点击书源展开分类时改为平滑展开，不再直接闪现。
- **大量分类展开更流畅**：分类较多时只构建当前可见内容，折叠后释放隐藏分类和动画资源，减少大量书源下的卡顿。
- **分类切换与滚动条修复**：选择分类后内容从顶栏下方稳定显示，不再从顶栏遮挡区滑入；返回书源目录时恢复原来的浏览位置，右侧滚动条也会避开顶栏和底部导航，保持可拖动。

### 字体体验

- **重构字体选择界面**：将 App 字体与阅读字体选择从设置主页面拆分为独立模块，统一系统字体、在线字体和自定义字体的展示与选择逻辑，降低设置页面复杂度。
- **在线字体下载更流畅**：下载进度继续以独立监听器节流刷新，只更新字体列表区域，避免网络数据分块触发整个设置页和应用根组件重复重建。
- **字体状态更清晰**：统一在线字体的待下载、下载中、校验、注册、已下载和失败状态展示，保留实时进度与失败重试入口。
- **字体故障更容易诊断**：字体清单损坏、字体加载失败和临时文件清理失败现在会保留诊断信息；不可用字体仍会安全回退到默认字体，不影响应用启动。

### 导入性能

- **减少大文件导入磁盘读取**：复制外部书籍时同步计算 MD5，复制完成后不再为校验目标文件额外完整读取一次。大文件导入仍保留内容一致性校验，同时减少磁盘 I/O 和等待时间。
- **导入失败回滚更可靠**：半成品书籍、封面和 Web 导入数据仍会尽力清理；清理失败不再覆盖真正的导入错误，并会留下可诊断信息。

### 架构与稳定性

- **拆分超大文件**：将 EPUB 元数据模型与解析逻辑从书籍导入服务中独立出来，并拆分设置页的字体选择和社区图标绘制代码，降低核心文件体量与职责耦合。
- **补充回归测试**：新增大文件复制哈希一致性、字体选择弹窗和在线字体下载进度测试，覆盖重构后的关键行为。

## v2.5.8 - 2026-08-09

版本：`2.5.8`，基础构建号：`260809001`。

### 界面优化

- **书源管理页布局优化**：优化书源管理页的界面布局，操作更清晰。
- **发现页搜索布局优化**：优化发现页搜索框的界面布局，体验更顺畅。

### 性能优化

- **重构文字排版引擎**：重构文字排版引擎，提升翻页与滚动性能。

### 书源修复

- **修复详情页误报 404**：部分书源在一个搜索/发现结果条目内命中多个链接（例如书名链接和下载链接同时存在）时，规则引擎会把命中的多个属性值拼接成一个畸形地址，导致打开该书详情时请求 404。现在单值规则命中多个节点时只取第一个匹配，和参考实现（jsoup `Elements.attr()`、XPath 1.0 节点集转字符串）的取值语义保持一致。
- **修复正文被误清空**：部分书源用 `replaceRegex` 清理正文中注入的水印/提示文字，例如 `##.*某段提示.*`。此前该正则按跨行匹配执行，遇到多段落正文会从开头一路匹配到提示文字结尾，把整章正文替换成空。现在改为按行匹配，只清理提示所在的那一行，不再影响其余正文。
- **修复目录章节顺序错位**：修复部分书源打开目录时章节顺序错位的问题。

### 其他修复

- **修复统计页卡片溢出**：修复详细统计页“总览”标签下数据卡片在部分屏幕宽度或系统字体放大时底部溢出的问题。

## v2.5.7 - 2026-08-05

版本：`2.5.7`，基础构建号：`260805005`。

### 账号修复

- **注册错误提示更准确**：注册时如果邮箱格式不正确、用户名或密码不符合要求，会直接显示对应的修改提示；验证码错误或已过期时提示重新获取，不再误报为网络连接失败；其他校验失败会显示“提交的信息不符合要求”，便于判断问题并重试。

## v2.5.6 - 2026-08-05

版本：`2.5.6`，基础构建号：`260805004`。

### 阅读体验

- **修复退出阅读后无法准确回到原页面**：退出 EPUB/TXT 阅读器时会保存当前实际可见的页面位置，即使正处于横向翻页动画中也以目标页作为续读位置；再次打开书籍或开启“启动时回到上次阅读”后会恢复到章节内的准确文本位置，不再经常回到该章节第一页；自动续读优先使用最新的阅读会话位置，避免旧进度覆盖刚保存的页内位置；优化多个保存时机同时发生时的写入顺序，减少互相覆盖进度的情况。
- **保持 EPUB/TXT 水平翻页跨章节时的稳定性**。

## v2.5.5 - 2026-08-05

版本：`2.5.5`，基础构建号：`260805003`。

### 阅读体验

- **进一步修复跨章节边界翻页闪烁**：在 v2.5.4 防止拖动过程中强制纠正位置的基础上，延迟章节窗口调整（前向收缩、后向扩展）直到滚动动画完全停止。此前 `ScrollEndNotification` 会在滚动动画仍在执行时触发窗口变更，导致 `bookPages` 在手势过程中重建，表现为短暂闪现错误页面（约 0.1 秒）。现在通过检查 `PageController.position.isScrollingNotifier.value` 确保动画真正结束后才修改章节窗口，彻底消除跨章节边界时的闪烁。

## v2.5.4 - 2026-08-05

版本：`2.5.4`，基础构建号：`260805001`。

### 阅读体验

- **修复水平滑动翻页在章节边界闪烁**：手动滑动横翻页（TXT、EPUB 等本地格式通用）在跨越章节边界时，页面控制器会在拖动仍在进行时被强制纠正到目标页，导致正文瞬间闪成另一页再闪回，并打断手势跟手。现在仅在没有活跃拖动/惯性滚动时才会做这种位置纠正；同时统一了 TXT 与 EPUB 在跨章节窗口维护上的时机，均延后到本次滑动结束后再执行，不再在拖动过程中改变可见页列表。

## v2.5.3 - 2026-08-04

版本：`2.5.3`，基础构建号：`260804011`。

### macOS

- **修复 Finder 无法打开应用**：macOS 应用内部可执行文件改用 ASCII 名称 `OpenReading`，Finder 和 LaunchServices 不再因中文可执行入口报“应用程序无法打开”。应用在 Finder、菜单栏和窗口中仍显示为“开元阅读”。
- **修复本地书籍导入失败**：从系统文件选择器选中书籍后立即保存到应用沙盒，避免 macOS 临时文件访问权限失效后无法读取；导入完成只清理应用自己的暂存副本，不会删除原文件。
- **发布流程新增真实启动检查**：签名、公证和 Gatekeeper 验证完成后，流水线会通过 LaunchServices 实际启动应用；无法从 Finder 正常启动时会阻止发布。

### EPUB 阅读

- **修复水平滑动跨章节闪页**：跨越上一章末页与下一章首页时，不再因分页缓存预热完成而瞬间跳到其他页；拖动和惯性动画期间保持页面 identity 与位置稳定，翻页更跟手。
- **修复仿真翻页无法跨章节后退**：现在可以从下一章首页正常翻回上一章末页。

## v2.5.2 - 2026-08-04

版本：`2.5.2`，基础构建号：`260804010`。

### 账号中心

- **iOS 与 macOS 支持 App Store 永久高级版**：Apple 平台登录后可通过非消耗型内购永久解锁，并提供恢复购买；购买验证成功后绑定当前开元阅读账号，和其他平台的高级版权益统一同步。购买入口改为更贴近 iOS 原生的样式，并修复了商品信息加载较慢时购买按钮卡在空白状态的问题，现在会显示加载中或失败重试。
- **iOS 与 macOS 支持原生 Apple 登录**：账号登录方式新增 Sign in with Apple，使用系统原生授权面板，与 Google/GitHub 等第三方登录同等展示；登录响应速度同步优化。
- **新增卡密兑换与邀请奖励**：Android、Windows、Linux 和 Web 平台可通过一次性卡密或邀请码/邀请链接解锁永久高级版；每个账号拥有固定邀请码，受邀用户首次兑换后邀请人也会获得奖励，账号中心可查看邀请统计与最近邀请记录。
- **新增原生账号入口**：设置页可进入登录、注册和用户中心，支持邮箱密码、邮箱验证码、找回密码、资料与头像修改，以及安全退出。
- **账号安全集中管理**：登录方式、邮箱、密码和可选的双重验证统一收纳到账号安全页；令牌保存在系统安全存储中，第三方登录通过系统浏览器完成设备授权。
- **安全操作改为分步页面**：更换邮箱、设置密码和双重验证分别进入独立页面；双重验证按邮件确认、验证器绑定和恢复码保存逐步引导，界面更清晰。
- **第三方登录更顺畅**：Google/GitHub 登录改为直达官方授权页，无需先跳转官网中转；授权完成后自动返回 App，无需手动切换。
- **修复 iOS 登录状态残留**：旧用户卡片缓存仍在、但安全令牌或会话已经失效时，不再误显示为已登录；进入用户中心会重新加载登录服务配置，Apple、GitHub、Google 和 Passkey 等可用登录方式不会继续全部置灰。
- **头像加载更稳定**：新增圆形取景裁剪与缩放，上传前可自由构图；头像支持本地缓存，减少重复加载，上传或删除后自动刷新，避免继续显示旧头像。

### 更多书源

- **支持需要登录或验证码的书源**：新增交互式登录能力，遇到登录墙或验证码的书源可在 App 内完成验证后继续使用。
- **新增手动整书换源**：书架中已导入的书籍可切换到另一个书源，阅读进度与笔记保留。
- **书源脚本引擎兼容性扩展**：新增 Cookie 脚本 API、HEAD 请求、脚本响应缓存、共享脚本库和 DOM 集合方法等能力，让更多第三方书源可以直接使用。
- **修复正文与封面兼容问题**：减少部分书源下正文被误判为空、远程封面加载失败的情况。
- **大型书源库更流畅**：书源较多时，打开、搜索和换源启动不再明显卡顿。

### 阅读体验

- **新增移动端省电模式**：可在设置中将刷新率上限限制为 60fps，降低高刷设备的阅读耗电。
- **新增 HarmonyOS Sans 官方字体**。
- **修复变量字体在 iOS 上的字重问题**：切换到思源宋体等变量字体时，字重调节偶发不生效、部分字形忽大忽小，现已修复。
- **选中文字工具栏统一为阅读器样式**：高亮、笔记等文字选择操作改用与顶部、底部控制栏一致的毛玻璃外观，在不同阅读主题下保持协调。
- **修复偶发空行**：TXT 与在线阅读在部分场景下不再出现多余的空行。
- **修复长篇小说 AI 预处理卡住**：合并阶段不再出现卡死或失败。
- **修复下载后打开卡死**：在线书籍下载完成后偶发打开卡死、转为本地阅读时进度换算错误的问题已修复。

### 页面体验

- **统一二级页面导航**：设置、同步、书源管理、任务、AI 历史和阅读主题等子页面改用玻璃质感顶栏；标题居中显示，左右圆形按钮保持对称，搜索与标签页保留在内容区域。
- **首次启动引导改为四段式**：软件介绍、使用协议、书源协议和隐私说明分别独立确认，过渡更流畅。
- **iOS 版本号跟随 Flutter 构建配置**：避免 Xcode 工程残留旧版本号覆盖当前版本。
- **macOS 最低系统版本调整为 13.5**：与新版 Passkey 登录组件的系统要求保持一致，避免发布构建在旧部署目标下失败。

### EPUB 阅读

- **目录锚点精确跳转**：完整保留 EPUB NCX 与 EPUB3 目录链接中的 `#fragment`，点击同一 XHTML 内的二级、三级标题时会跳到对应正文位置，当前目录小节也按真实锚点和阅读位置高亮。
- **修复阅读进度偶发回退**：翻页产生的异步进度写入现在严格按顺序落库，主动退出阅读器时会等待最后一次位置保存完成，避免下次打开退回前面数页。

## v2.5.0 - 2026-08-03

版本：`2.5.0`，基础构建号：`260803003`。

### 性能与缓存

- **发现页和书源列表加载更快**：为书源清单、推荐、分类、发现频道、分页列表和书籍详情增加分层缓存。重复进入页面或前后翻页时优先复用已有数据，减少重复联网、等待和界面抖动。
- **并发请求自动合并**：同一时间发起的相同请求只执行一次，其余调用共享结果；磁盘缓存改为后台有序写入，不再阻塞页面展示。下拉刷新会准确清除对应缓存，确保刷新后拿到最新内容。
- **首页和书库更流畅**：首页最近阅读改为批量查询，书库列表复用已完成的筛选与搜索结果，普通界面刷新不再反复扫描整座书库。
- **启动阶段减少后台争抢**：取消启动时自动执行的全书库路径修复和文件清理，避免与导入、下载、同步及用户操作抢占磁盘；需要修复的本地书籍会在打开时按单本处理。

### 发现页

- **新增列表布局**：发现页可在标准布局与列表布局之间切换，根据浏览习惯选择更紧凑的信息展示方式；书源和频道按需加载，大型书源库下也能保持流畅。

### 更多书源兼容

- **书源变量可跨页面延续**：搜索阶段生成的书籍变量会继续传递到详情、目录、正文、下载和换源验证，依赖动态变量拼接地址的书源不再出现搜索正常、打开失败的问题。
- **复杂章节请求更可靠**：章节地址会保留 POST、请求头和后台网页等选项，Android 后台网页会等待导航稳定后再读取正文。
- **正文提取更完整**：正文规则匹配多个节点时会按段落换行合并，清理规则默认不再跨行误删整章内容。

### AI 阅读助手

- **自定义服务商可选择协议**：添加自定义 AI 模型时，可明确选择 OpenAI Compatible 或 Anthropic 协议。应用会按协议生成正确的请求地址、认证头、请求体和响应解析规则。
- **Base URL 提示更清楚**：配置页会根据所选协议提示 `/v1` 的填写方式，并自动避免路径重复，降低自定义服务配置错误的概率。

### 阅读体验

- **新增字体粗细调节**：阅读设置支持调整正文字体粗细，可根据字体、屏幕和个人阅读习惯选择更清晰或更轻盈的显示效果。
- **修复首次阅读翻页闪烁**：新导入书籍首次打开后，仿真翻页不再因为后台快照缓存逐张生成而反复闪动。同一次翻页会锁定使用同一组页面纹理，新缓存只在后续翻页中启用，冷缓存和热缓存下的画面保持一致稳定。

## v2.4.6 - 2026-08-02

版本：`2.4.6`，基础构建号：`260803001`。

### 更多书源

- **大型书源库快速导入**：书源 JSON 改为本地解析、去重并批量写入，导入时不再逐条联网检测，数千个书源也能快速完成导入。
- **原生支持更多书源规则**：扩展搜索、发现、详情、目录和正文规则执行，支持常用脚本、CSS、XPath、JSONPath、Cookie、GBK、重定向与动态网页处理，让更多书源可以直接使用。
- **书源查找与管理**：发现页和书源管理页支持搜索书源，改进筛选、展开和错误重试，并自动清理简介中的多余换行、标签与符号。

## v2.4.5 - 2026-07-31

版本：`2.4.5`，基础构建号：`260731002`。

### 悬浮导航栏

- **尺寸可自定义**：设置 → 悬浮导航栏新增「尺寸」选项，可在自动适配与自定义之间切换。
- 自定义模式下可分别调整导航栏高度和左右边距，设置页面会实时预览调整效果。

### 书库

- **支持多选删除**：书库页现在可以进入多选模式，一次选择并删除多本书籍，批量管理更加方便。

### 大书导入与 EPUB 排版

- **单本导入上限提高至 500 MB**：取消原有 100 MB 限制，可直接导入更大体积的本地书籍。
- **EPUB 按需解析**：重做 EPUB 解析流程，不再在导入或打开时把整本书及全部图片一次性载入内存，显著降低大型 EPUB 的内存占用与等待时间。
- **优化 EPUB 排版算法**：改进章节、样式、图片与目录处理，对大型书籍和复杂 EPUB 的兼容性更好，支持更多 EPUB 文件正常导入和阅读。

### 书籍打开动画

- **新增四种打开动画**：书库设置新增「极简淡入」「纸张上浮」「横向滑页」「书本展开」，可按喜好自由选择。
- 优化动画时序与渲染路径，书籍打开响应更迅速，封面、加载状态与正文之间的过渡更加流畅。

### 更多书源格式

- **支持导入通用阅读书源**：书源管理现在可识别并导入通用阅读书源 JSON，同时保留原有 Open Reading 书源协议支持。
- 导入前会分析书源兼容性并区分完整支持、部分支持与暂不支持的规则；为保障安全，新导入的书源默认关闭，可在设置 → 书源管理中确认后自行开启。

## v2.4.4 - 2026-07-31

版本：`2.4.4`，基础构建号：`260731001`。

### 修复

- **修复部分 EPUB 打开卡死**：修复某些内部被拆分为数千个碎片文件、且同一张插图（如页头 logo）被大量章节复用的 EPUB 打开时长时间无响应甚至接近卡死的问题。阅读器解析 EPUB/Kindle 正文时，每张图片现在只解码一次并在所有引用它的章节间共享，不再随每次引用被整份复制；受影响的书籍打开耗时从数分钟量级降到与普通书籍一致的数秒。

## v2.4.3 - 2026-07-29

版本：`2.4.3`，基础构建号：`260729001`。

### 修复

- **切换 AI 模型即时生效**：修复在设置里切换 AI 模型后，已打开的对话界面仍使用旧模型的问题。阅读器「问AI」面板与 AI 页现在每次发送都会读取当前激活的模型与配置，无需重启应用。
- **iOS 系统默认字体一致**：修复 iOS 中文阅读使用「系统默认」字体时，翻页后可能出现相邻汉字粗细、视觉大小不一致的问题。本地书籍与在线书源阅读器现在稳定使用系统自带的苹方中文回退字体，不新增字体下载。
- **仿真翻页支持左侧起滑**：修复从屏幕左半边向左滑无法翻到下一页的问题。手机单页仿真翻页现在按实际滑动方向判断前后页，从任意横向位置向左滑都可翻下一页，同时保留右边缘起滑的即时跟手效果。

## v2.4.2 - 2026-07-27

版本：`2.4.2`，基础构建号：`260727001`。

### 阅读体验

- **覆盖翻页**：翻页模式新增「覆盖翻页」。页面像一摞纸的最上层：向前翻时当前页向左划出、露出底下静止的下一页，回翻时上一页从左缘盖回；移动页右缘带渐变阴影，底页随揭开逐渐变亮。拖动实时跟手，松手按速度用弹簧动画完成翻页或回弹，首末页有橡皮筋提示；点击翻页与音量键翻页同样生效。本地 TXT/EPUB 与在线书源阅读器均可选择，平板双页布局下整个对页作为一张纸整体滑动。
- **点击区域设置**：阅读设置新增「点击区域设置」，把屏幕分成 3×3 九宫格，每个区域可自定义为上一页、下一页、上一章、下一章、菜单或无操作，习惯左手翻页可以把左列设为下一页。入口点击后收起控制栏，直接在当前阅读页上全屏预览九宫格，点哪格改哪格，可一键恢复默认；至少保留一个菜单区域，全部移除时中间区域自动恢复为菜单。默认布局与旧版一致（左列上一页、中列菜单、右列下一页），本地书籍与在线书源阅读器共用同一配置。
- **启动时回到上次阅读**：设置 → 阅读设置新增「启动时回到上次阅读」开关（默认关闭）。开启后，在阅读中退出或应用被系统回收，下次启动会自动打开当时的书并回到上次阅读位置；正常返回书架后再退出则不会打扰，启动仍停留在首页。本地书籍、漫画、PDF 与在线书源阅读器均生效；点通知或从外部文件打开进入应用时，显式意图优先，不会被自动恢复抢占。
- **阅读设置重新排版**：阅读页的「阅读设置」弹窗从一列长滚动列表改为「主题 / 文字 / 版式 / 翻页」四个页签。主题页签集中主题卡片与顶部栏样式；文字页签保留字号、行高与对齐，字间距、首行缩进、段间距收进默认折叠的「高级排版」；版式页签管理左右与上下边距；翻页页签集中翻页模式、平板双页、点击翻页动画、点击区域与下拉书签。每个页签基本无需滚动，本地书籍与在线书源阅读器共用同一面板。

### AI

- **阅读时问 AI**：阅读器控制栏新增「问AI」按钮，选中文字的弹出工具栏同样新增「问AI」。发起提问会自动携带当前页正文或选中段落及其上下文，可在面板内连续追问；回答按 Markdown 渲染（标题、列表、代码块、链接），对话样式跟随阅读主题。本地书籍与在线书源阅读器均可用。
- **AI 页**：底部导航新增「AI」页，进入即为对话界面，悬浮玻璃输入条贴合键盘。输入框左侧加号可「关联书籍」——回答会参考该书的本地知识库摘要与你的笔记、高亮；顶栏右侧历史入口可回看全部对话，点任意一条即载入继续聊，左滑删除单条、一键清空全部。阅读器里的问答同样计入历史。
- **AI 预处理书籍（本地知识库）**：设置 → AI 阅读助手新增「AI 预处理书籍」开关（默认关闭，开启前校验模型可用并明确提示 token 消耗），书架长按书籍也可手动触发。AI 会分块通读整本书并生成 Markdown 摘要，作为该书的本地知识库供对话注入上下文；任务在后台队列串行执行，「下载任务」页新增「AI 预处理」标签页查看进度、取消与清理。TXT / EPUB / Kindle（MOBI/AZW/AZW3）支持预处理。
- **模型管理**：AI 阅读助手设置整理为独立页面与竖排模型列表，当前启用项打勾标示，长按可编辑或删除；预置 OpenAI、Claude、Gemini、GLM、MiniMax 及 DeepSeek、Qwen 等 OpenAI 兼容服务模板，也可自定义 Base URL 与模型名。修复了保存模型配置时可能整页崩溃的问题。
- **预处理与对话并发**：AI 预处理书籍时，阅读器「问 AI」与 AI 页对话不再受影响。对话请求现在优先执行：预处理队列在每次分块请求之间主动让行，等对话结束再继续，不会挤占服务商按 API Key 的并发额度；整本书的章节抽取（TXT 解码、EPUB 解包、Kindle HTML 转纯文本）也移到了后台 isolate 执行，修复了预处理启动时大书卡住整个界面的问题。
- **预处理更抗限流**：分块请求不再背靠背连发——相邻请求之间保留 1.5 秒间隔；遇到限流（429/529）、服务端故障（5xx）或网络传输失败时按 5s/15s/45s 退避自动重试，最多 3 次，重试等待期间取消任务立即生效。API Key 无效、模型不匹配等配置错误仍然立即失败并展示原因，不做无意义的重试。

### 界面与导航

- **导航页面可隐藏**：悬浮导航栏设置里每个页面新增显示开关，不常用的页面（包括首页）可以直接隐藏，设置页锁定不可隐藏；顺序照旧可拖动调整，恢复默认会同时还原顺序与可见性。键盘弹出时悬浮导航栏自动滑出隐藏，不再被键盘顶起。
- **书架长按菜单**：由大卡片重排为紧凑列表行，一屏放得下全部操作，并新增「AI 预处理」入口。

### 漫画与 PDF

- **更多漫画格式**：新增 CBT（TAR 容器）漫画直接阅读，与 CBZ 共用同一套漫画阅读器与按需解压管线。打开漫画时按文件头识别真实容器——市面上大量 CBR/CB7 其实是改名的 ZIP 包，这类文件现在可以直接阅读；真正的 RAR/7z 压缩包会给出明确的本地化「转换为 CBZ」提示。CBT/CB7 进入文件选择器与目录扫描导入，所有漫画格式导入时都会尝试提取真实页数与第一页封面。
- **完整的控制栏**：漫画与 PDF 阅读器的控制层全面升级。底栏新增上一页/下一页按钮、可拖动进度条与「跳转页码」输入，顶栏页码同样可点击直达任意页；新增按书记忆的「阅读方向」，日漫可切换为从右到左翻页，进度条与翻页按钮同步镜像。设置面板提供页面背景色（黑/灰/白）、「保持屏幕常亮」与「音量键翻页」开关，后两项与文字阅读器共用同一偏好。
- **点击区域与音量键**：漫画与 PDF 现在使用与文字阅读器相同的 3×3 点击区域配置——点右侧翻下一页、点中间呼出菜单，均可在点击区域设置中自定义（从右到左模式下左右列自动镜像，符合日漫习惯）；Android 音量键翻页同样生效。

## v2.4.1 - 2026-07-26

版本：`2.4.1`，基础构建号：`260726006`。

### 更多格式可以直接阅读

- **Kindle（MOBI/AZW/AZW3）**：无 DRM 的 Kindle 书籍现在可以直接打开阅读。KF8 按 skeleton 结构分章，旧版 MOBI 按分页符切章，正文保留标题层级、粗斜体样式与内嵌插图；导入时读取 EXTH 真实书名、作者、简介、语言、ISBN、主题标签与内嵌封面（此前大文件因头部截断丢失封面的问题一并修复）。DRM 加密书籍会给出明确的本地化提示，元数据与封面仍可正常导入；Web 端暂不支持 Kindle 解析。
- **CBZ 漫画**：新增专用漫画阅读器，支持左右翻页、双击/双指缩放（缩放中锁定翻页）、点击呼出标题栏与进度条跳页、阅读进度记忆与相邻页预载；isolate 内按需解压单页加 LRU 页缓存，大体积漫画不整本驻留内存。导入时以第一页真实图片作为封面并统计真实页数。CBR 暂不支持阅读，打开时提示先转换为 CBZ。
- **PDF**：新增应用内 PDF 阅读（Android/iOS/macOS/Windows/Web），按屏幕像素密度渲染清晰位图并自动铺白底，页渲染串行排队避免平台并发限制；翻页、缩放、跳页与进度记忆与漫画阅读器共用同一套交互。pdfx 引擎暂无 Linux 实现，Linux 端打开会明确提示。
- **HTML / Markdown**：阅读器此前已支持渲染，现在可以从文件选择器与系统「打开方式」导入书架。

### 书架

- 长按书籍新增「自定义封面」与「恢复默认封面」：挑选本地图片（jpg/jpeg/png/webp/gif/bmp，20MB 以内）替换封面，原封面自动备份、可一键无损还原；删除书籍时自动清理相关封面残留文件。

### 其他

- 格式能力注册表与 `docs/book-format-support.md` 全面对齐真实能力，FB2/RTF/DOCX 的能力标注修正为「可读」；过时的「仅支持 EPUB 和 TXT」提示更新为通用文案。
- 新增与修订文案覆盖简体中文、繁体中文、英文、日文。
- 全量回归：Flutter 测试 564 项通过，静态分析无 error/warning；新增 Kindle 图片引用重写、CBZ 解析全链路与翻页阅读骨架的单元/组件测试。

## v2.4.0 - 2026-07-26

版本：`2.4.0`，基础构建号：`260726005`。

### 阅读标注与文字笔记

- 本地文件与在线书源阅读器均支持选中文字后高亮、下划线、复制和添加引用批注；选区工具栏、颜色面板与批注输入框完整适配当前阅读主题。
- 文字批注会在原文下方显示可点击的虚线标记，轻点被标记的文字即可查看引用原文和笔记内容，同时不会误触翻页。
- 高亮与文字批注统一使用章节 UTF-16 offset、`TextAnchor` 和 `CanonicalLocator` 定位，重新分页、调整字号或行距后仍可按原文锚点恢复。
- 目录面板新增“笔记”页，可按真实章节顺序浏览高亮、下划线和文字批注，支持点击定位与删除。
- SQLite 升级至 v20，`book_notes` 增加稳定 UUID 和类型载荷；迁移会幂等修复旧记录、保护身份唯一性，并保留未来 WebDAV 笔记恢复所需的兼容字段。当前笔记/高亮 WebDAV 能力仍保持关闭。

### 连续听书与 Android 媒体控制

- 新增从当前阅读位置开始的连续听书：支持暂停恢复、上一句、下一句、自动跨章、页面跟随和精确进度保存。
- 当前正在朗读的句子会在正文中实时高亮；暂停时保留、停止时清除，并可与已有高亮和文字批注同时显示。
- 新增 OpenAI-compatible 云端 TTS，可配置 HTTPS Base URL、模型、音色与音频格式；API Key 使用系统安全存储，云端失败可自动回退系统语音。请求禁止重定向、限制响应大小，并使用会话内缓存减少重复合成。
- 听书面板最高占屏幕 72%，支持拖动顶部横条下拉关闭，长内容在面板内滚动；定时关闭可自由选择小时和分钟，并显示剩余时间与进度，不再写死固定档位。
- 播放中调整语速、音量、音调、音色或云端模型会立即生效，并从当前句的实时位置继续，无需先暂停再播放。
- Android 使用 `mediaPlayback` 前台服务和 framework `MediaSession`，通知操作、耳机及外设媒体键均可控制当前朗读会话。
- Android 16 在系统允许时使用 `Notification.ProgressStyle` 请求实时进度展示；未获得 promoted ongoing 资格时自动保留普通持续通知与 MediaSession 控制。

### WebDAV 在线书源与书架恢复

- WebDAV v1 新增稳定的 `book_sources` 数据集，同步用户注册的书源公开信息、启用状态与添加时间；密码、令牌和其他凭据不会上传。
- 在线书架记录现在携带书源与书籍快照，可在其他设备恢复为可直接打开的在线书；在线记录删除不会误删已经下载到本地的同源书籍。
- 阅读进度同步新增在线章节 ID、章节序号和章内进度，并在恢复后刷新书架与首页；章节正文、目录、封面路径和设备缓存继续只保存在本机。

### 阅读器交互与兼容性

- 选区、长按、拖选、批注点击和翻页轻点现在使用清晰的单一手势所有权，避免一次点击被处理两次或长按选区误触翻页。
- 修复启用文字选择后仿真翻页偶发失灵的问题：右侧轻点现在容忍正常手指抖动，从右向左拖动也不再被文字选择手势抢占；长按选字仍会优先进入选择模式。
- 修复首行缩进设为零时，隐藏的原文空白被错误保存进选区的问题，同时继续保证分页 canonical 范围完整连续。
- 修复旧批注缺少 canonical locator 时错误跳到第一章的问题；现在会按章节 ID、章节标题和旧字段逐级回退。
- 阅读标注、听书、云端 TTS、WebDAV 在线书同步、分页与两套阅读器已纳入全量回归；当前全量 Flutter 测试 545 项通过，静态分析无 error/warning。

## v2.3.9 - 2026-07-26

正式版本：`2.3.9`，基础构建号：`260726004`。

### 书籍打开动画流畅度

真机 VM 时间线实测定位并修复了三处让打开动画掉帧的主线程冻结，本地与在线书源阅读器均已修复，冷开与重复开关测试稳定在无掉帧：

- 整章排版、章节文本解码与正文首帧构建（单次可达 50~100ms）此前会落在封面飞行的运动阶段执行；现在统一延后到封面到达静止画面之后，已排版过的书仍走原缓存快路径立即显示。
- 正文渐显阶段透明度每帧变化会反复重建整页无障碍语义树（单帧约 9ms，足以击穿 120Hz 帧预算）；语义树现在等打开动画完全落定后一次性构建。
- 封面在视觉上 52% 处就已停止运动，但底层路由动画仍继续行进到 100%、每帧持续请求新帧；此前把 52% 当作安全窗口起点，若正文恰好在这段时间到达仍会同步整章排版、拖慢同时播放的其它动画（如首页悬浮导航收起）。现在等路由动画彻底停止请求新帧才进入安全窗口，消除了这一概率性掉帧。
- 相邻章节预热排版与系统栏切换统一延后到整个打开动画（封面飞行 + 正文渐显）落定之后。

### 顶部信息与沉浸阅读

- 新增顶部信息样式「灵动信息栏」：借用被隐藏的系统状态栏区域显示时间与电量，不再挤占正文排版空间；随翻页动画一起移动（画在纸页内部，随卷页/滑动同步），不再是锁死屏幕的悬浮层，左右间距加大以避开圆角屏幕。
- 「完全沉浸」在上下滚动模式下彻底取消顶部与底部的预留区域，正文铺满整个屏幕；系统状态栏与灵动信息栏两种样式下也不再额外预留章节标题条的空间，只保留状态栏本身高度，把多余空间还给正文。

### 阅读设置面板

- 修复设置面板高度过高、几乎遮住整个屏幕的问题：面板最大高度收窄为屏幕高度的一半。
- 调整面板内容顺序：字体大小、行间距、字间距、对齐、首行缩进、段间距和页边距等排版滑块提到最上方；主题选择居中；翻页模式、顶栏样式、下拉书签、翻页动画等开关移到最下方，需下滑查看。

### 书库

- 书籍详情新增「重命名」：修改书名的同时，若书籍存在本地文件会同步重命名磁盘文件（自动清洗非法字符、避免覆盖同名文件）；仅在线、尚未下载的书籍只更新书名。
- 在线书源书籍的书籍信息不再把内部进度换算值误当作页数展示，改为显示真实章节数。

## v2.3.8 - 2026-07-25

当前测试构建：`2.3.8+260725020`。

### 阅读排版

- 统一阅读器的字体初始化与缩放计算，保持不同阅读入口、不同平台及重新分页后的字号表现一致。

## v2.3.7 - 2026-07-23

当前测试构建：`2.3.7+260723012`。

### 书籍打开、加载与返回动画

- 优化书库页书籍打开与关闭动画：首页悬浮导航在打开时立即向下滑出，退出阅读的第一帧立即从下方回弹，Android 预测性返回取消后会重新收起，不再等待页面切换结束。
- 增加底部悬浮导航栏在书籍打开、关闭时的回弹动画，使导航栏退场与复位更自然，并与阅读页转场保持同步。
- 有稳定封面的入口使用先快后慢的封面展开与缩回曲线。打开时封面在正文真正绘制完成前保持可见，再与正文直接渐隐渐现；退出时正文透明度跟随返回手势进度提前交给封面，不再硬切或闪出白屏。
- Android 14+ 侧滑返回直接驱动书籍缩回动画，并加入跟手预测：动画跟随手指进度，松手提交后从当前位置继续，取消则平滑恢复全屏阅读。
- 美化书籍加载动画，慢加载时按“封面 → 主题化书本加载动画 → 正文”分两段交叉渐变，三段交接时间均已延长。加载期间点击阅读区域仍可呼出标题栏和控制栏退出。
- 优化首页打开书籍动画：继续阅读等无法稳定取得封面位置的入口改用纸面纵向接力和双阶段淡入，不再缩放整张阅读页面；开启“减少动态效果”时取消位移。
- 退出动作开始时把当前阅读屏冻结为纹理快照，后续缩回只变换这一张图片，不再逐帧合成大量正文、选择层和控制层；预测性返回取消时使用同一快照恢复到全屏，再切回实时正文。
- 退出动画期间暂停仿真翻页快照和高成本刷新，书架书名、进度与统计在路由动画真正结束后再更新；封面位置尚未布局时安全降级，避免偶发一两帧红色错误页。

### 发现页书籍详情

- 优化发现页书籍详情的布局、信息层级与开合动画；从详情进入阅读器时使用连续的纸面接力，减少页面切换割裂感。
- “加入书架”不再关闭详情后弹出第二个面板，加入方式、提交、下载进度、成功、已存在和失败重试统一在同一个动画状态流中。
- 加入成功后播放小封面落入书架的反馈动画并自动收起；重复加入给出清晰提示，失败时保留详情面板以便重试。

### 目录与章节滑动性能

- 修复打开超长目录卡顿：目录展示模型提前生成，折叠可见性计算改为单次线性扫描，首次打开、搜索和定位更流畅。
- 修复目录跳转卡顿：大型 TXT 按章节索引异步读取目标内容，并预取相邻章节，不再在界面构建过程中同步读取整段文件。
- 修复在线书籍章节标题页水平滑动卡顿：正文转换、相邻章节分页与缓存写入移出关键动画路径，标题页和正文之间的连续横滑更稳定。

### 超大 TXT 打开性能

- 几十 MB TXT 的每一个超大章节都会切成约 32K 字符的懒加载片段，不再只处理“完全没有章节”的文件；正文只异步读取当前和必要的相邻小片段，避免某个被识别出的巨型章节继续阻塞主线程。
- 第一次生成大文件索引的 CPU 与存储重活延后到封面→加载动画结束后启动；已有索引也要等封面飞行动画落定后再读取和准备章节窗口，但不追加首次索引的 800ms 等待，修复缓存越大、再次开书反而越掉帧的问题。
- 修复入口路由完成标志短暂失真时把仍在前进的开书动画误判为取消、进而显示“没有可以阅读的正文”的问题；是否取消现在只以实时路由动画状态为准。

### Android TXT/EPUB 导入

- Android 已授权目录继续只使用标准 SAF `DocumentsContract` 递归扫描；系统文档提供器临时返回空列表时不再尝试越权或不可靠的 direct-child、MediaStore、文件描述符回退。
- SAF 文件物化移出 Android 主线程，完成后必须与目录扫描声明的文件大小一致，再进入源哈希、复制和副本哈希流程，避免空文件或截断文件被当作导入成功。

### 阅读排版

- 修复段落间距或原文空行恰好落在分页边界时，新页面第一行显示为空白的问题。现在无论段落间距设置为 0、1 或 2，每页都会从首个可见内容开始显示，同时保留正文中的段落间距和首行缩进。
- 页首被折叠的空行仍保留在原文位置映射中，书签、阅读进度和重新排版后的定位不会发生偏移；章节末尾的连续空行也不会再生成独立空白页。

### 主题与设置

- 合并“应用主题”与“强调色”：强调色成为应用配色的统一来源，浅色与深色模式由同一颜色生成，并自动迁移旧主题配置。
- 重大优化设置页，合并并收纳多项设置选项；悬浮导航栏与书库布局改为独立详情页，主设置页更简洁，相关选项更集中。

## v2.3.6 - 2026-07-23

正式版本：`2.3.6`，基础构建号：`260723001`。

### 在线书源与阅读连续性

- 已读书源的目录和正文会优先从本机缓存恢复；目录超过 30 分钟、正文超过 12 小时后在后台刷新，30 天内缓存可在书源变慢或离线时继续阅读。整本下载仍会强制获取最新正文。
- 首页打开在线书籍时会直接进入书源阅读器，不再按本地文件处理。
- 在线书源的水平滑动模式会提前下载并分页相邻章节，把前后章节的全部页面接入同一条连续翻页序列；章节标题与正文之间可像普通页面一样连续前后翻动，快速连续翻页或重新接管动画时不再在章节边界回弹。

### 在线字体下载更流畅

- 大型在线字体的文件签名和 SHA-256 完整性校验改在后台执行；下载进度只刷新字体列表，不再反复重建应用根界面与设置页。

### 书库网格阅读信息

- 书库网格新增“显示书名和进度”开关，默认关闭并继续保持纯封面布局。
- 开启后，每本封面下方显示单行书名、精简进度条和百分比；过长书名自动以省略号截断，网格行高保持一致。

### WebDAV 原文件与可读目录

- 新上传的书籍以未加密的原始字节、原文件名和扩展名保存到 `books/<书名 - 作者>/<原文件名>`；SHA-256 只保留在同步元数据和本地索引中用于完整性校验。
- 同名同内容文件直接复用，同名但内容不同的书使用 `(2)`、`(3)` 可读编号，避免覆盖已有文件。
- 旧版 `blobs/books/sha256/...` 文件继续正常下载，无需重新同步；检测到旧记录时，书籍文件页面会显示兼容说明。

## v2.3.5 - 2026-07-22

正式版本：`2.3.5`，基础构建号：`260722009`。

### 更直接的首页

- 首页重新聚焦“继续阅读”：最近一本书直接展示封面、作者、阅读进度和继续阅读入口，其他最近阅读以轻量封面列表呈现。
- 今日、本周、累计阅读时长与七日阅读节奏合并为一张统计卡；移除首页的每日目标、阅读计划、专注计时和 AI 阅读建议，不再为首页初始化对应服务。
- 手机与平板、桌面端统一复用同一套响应式首页实现，并补充 320px 手机与 1280px 宽屏布局回归。

### 浏览器版本与自动部署

- 浏览器端数据库接入基于 IndexedDB 的 SQLite 兼容层，并跳过仅适用于本地文件系统的图片目录、路径修复和应用内更新检查，使 Flutter Web 可以稳定启动并保留结构化数据。
- GitHub Release 流水线新增 Flutter Web 构建、版本元数据校验和内部部署产物；发布后通过受限 SSH 账号原子切换官网 Web 版本，并校验归档摘要与线上 `version.json`。
- Web 部署保留最近版本目录，上传、解包和软链接切换均限制路径、所有者、权限和归档内容，失败时不会覆盖当前线上版本。

### 平台发布与界面细节

- macOS 发布流水线支持 arm64 / x86_64 Universal App、Hardened Runtime、Developer ID 签名、Apple 公证、staple 和 Gatekeeper 验证；正式功能仍由受保护发布环境显式启用。
- 删除本地书籍导入时残留的联网封面搜索；EPUB/PDF 优先使用文件内封面，TXT/MOBI 等无封面格式直接在本地生成默认封面。
- 设置页移除与阅读无关的开发者产品推广，并压缩 AI 快捷模型卡片的高度、间距和状态表达，让设置入口更集中。
- 统一书库纯封面网格的裁切规则，并继续调整 Android 启动图标安全区和 iOS 旧系统回退资源的视觉比例。

## v2.3.1 - 2026-07-22

Android 版本号：`2.3.1`，基础构建号：`260722005`；iOS TestFlight 构建号：`260722007`。

### WebDAV 稳定性与选择权

- 当前同步范围聚焦书架信息、阅读进度、书签和阅读统计；笔记/高亮暂不出现在同步界面，但保留稳定协议身份和未来幂等迁移路径。
- 新导入书籍支持“每次询问（默认）/ 自动上传 / 始终手动”三种文件策略，不会因为开启 WebDAV 就静默上传全部书籍。
- 增强 Android 文件临时不可访问和 WebDAV HTTP 409 兼容处理，并在失败时显示连接、扫描、读取、合并或上传等具体阶段。
- 书籍文件同步会同时传输持久封面；跨设备恢复后以同步元数据校正书名和作者，不再把 TXT 第一章标题误当书名，也不会使用错误的本机重建封面。
- 连接配置与同步内容拆分为独立页面；阅读进度等范围开关修改后立即保存并从下一次同步生效。

### 缓存与书源封面

- 新增安全缓存管理，只清理书源封面、在线章节和临时更新文件，不触碰书籍、进度、数据库、设置或凭据。
- 书源封面使用并发受限、可重试的内存与磁盘缓存；加入书架时保存持久封面，损坏缓存可自动驱逐并重新获取。

### 下载任务取消

- 下载任务列表和下载进度弹窗新增单任务取消；等待中的任务不会开始，下载中的任务会终止当前网络请求、移除前台通知并继续处理下一条队列任务。
- 取消状态与下载失败分开显示，取消发生在文件落盘阶段时会清理未完成文件。

## v2.3.0 - 2026-07-22

Android 版本号：`2.3.0`，基础构建号：`260722003`；iOS TestFlight 构建号：`260722004`。

### ColorOS 流体云与实时下载

- 支持 ColorOS 流体云：书籍与官网 APK 下载过程中，可在流体云和状态栏持续显示实时进度与百分比短状态，离开当前页面后仍能查看下载状态。
- 底层使用 Android 16 原生实时进度通知并请求 promoted ongoing 展示；系统版本、设备或 OEM 未提供流体云展示时自动回退为普通进度通知，不影响后台下载。

### WebDAV 跨设备同步

- 新增本地优先的 WebDAV 同步，可将书架信息、阅读进度、书签、笔记、高亮与阅读统计同步到用户自己的 WebDAV 空间；支持手动同步以及应用启动、回到前台时自动同步。
- 离线修改会先可靠保存在本机，网络恢复后继续上传；多设备变更使用稳定设备身份、混合逻辑时钟、删除标记和记录级合并，避免简单整库覆盖造成数据丢失。
- 新增同步状态、待处理数量、最近同步时间、同步范围、活动结果和分类错误提示；损坏的远端批次、异常时钟偏差或不满足安全要求的服务不会被静默应用。

### 书籍原文件同步与连接安全

- WebDAV 书籍文件传输采用明确授权和逐本选择：只有开启“允许上传书籍原文件”并主动选中的书才会上传；其他设备可查看远端可用文件并按需下载。
- 书籍文件按 SHA-256 内容寻址并去重，当前单文件安全上限为 100 MiB；远端只有书架记录、尚未下载原文件时会明确显示“云端”状态，不伪装成本地可读文件。
- WebDAV 密码只保存在当前设备的安全存储中；连接默认要求 HTTPS，并限制凭据重定向到同一服务源。界面明确说明首版不是端到端加密，WebDAV 服务提供方仍可能读取远端内容。

### 书籍导出、文件关联与系统分享

- 书库本地书支持长按导出：Android 默认复制到公共 `Download/开元阅读/`，iOS 与桌面端使用系统保存面板；导出不会改变书库原文件和数据库路径。
- Android 可从文件管理器或其他应用直接打开、单文件分享或多文件分享 TXT/EPUB；单本导入后直接阅读，多本进入导入队列。iOS、macOS 与 Linux 接入各自系统文件打开入口，Windows/Linux 启动参数进入相同安全导入链路。
- 外部文件先物化到受控暂存区，再执行扩展名、MIME、文件头、数量和大小校验；冷启动、热启动与异常恢复统一按持久化 FIFO 处理。

### 书库布局

- 书库新增“卡片 / 网格”布局切换：卡片继续展示书名、作者和阅读进度，网格专注显示封面。
- 手机网格可选择每行 2 本或 3 本；平板和桌面端按相同封面密度响应式增加列数，旋转窗口后自动重新排布。

### 书源管理界面

- 重做手机端书源卡片布局，名称、描述、能力标签、启停状态和操作按钮分层排列，长名称和大量能力标签不再被横向控件挤到无法阅读。
- 手机端“添加书源”改为键盘自适应的全宽底部面板，地址、第三方责任说明、确认勾选和连接操作拥有独立空间；平板与桌面端继续使用宽屏对话框。

### 无阻塞应用提示

- 更新检查、用户协议、书源、字体、同步等页面的短反馈统一改为顶部浮层；手机顶部居中、宽屏右上显示，连续提示直接替换并自动退场。
- 提示浮层支持点击穿透、无障碍 live region 和减少动态效果，不再用底部提示条遮挡阅读或页面操作。

## v2.2.10 - 2026-07-22

Android 版本号：`2.2.10`，基础构建号：`260722002`。

### Android 16 实时下载通知

- 书籍与官网 APK 下载在 Android 16+ 使用系统原生 `Notification.ProgressStyle` 展示实时进度和百分比短状态，并请求 promoted ongoing 状态栏展示。
- Android 15 及以下继续使用普通进度通知；系统或 OEM 未提升展示时自动回退，不影响后台下载。

### 书籍文件导出与系统打开

- 书库中的本地书籍支持长按导出；Android 默认保存到 `Download/开元阅读/`，iOS 与桌面端使用系统文件保存面板。
- TXT 和 EPUB 可以从系统文件管理器或其他应用直接选择开元阅读打开；Android 同时支持单文件和多文件分享，单本导入后直接进入阅读，多本进入导入队列。

## v2.2.9 - 2026-07-22

Android 版本号：`2.2.9`，基础构建号：`18150`。

### 首行缩进

- 修复两端对齐的长段落在自动换行时，Flutter 排版引擎裁掉纯前导空白而导致首行缩进不可见的问题；短段、长段和跨页段落现在保持一致。
- 中文/英文引号开头以及原文带一个或多个空格的段落，统一按阅读设置显示缩进；TXT、EPUB、HTML、Markdown、FB2、RTF、DOCX 与在线书源同步生效。

### Android 官网更新安装流程

- 修复官网 APK 下载完成后只显示通知、不自动拉起系统安装器的问题；完成大小、SHA-256、包名、版本号和签名校验后立即进入原有安装流程。
- 保留下载完成通知作为安装请求被忽略或应用退到后台时的重试入口。

## v2.2.7 - 2026-07-22

Android 版本号：`2.2.7`，基础构建号：`14138`。

### 全格式首行缩进修复

- 修复 TXT 中 Unicode 换行符只产生视觉换行、没有被阅读布局识别为段落边界的问题；CR/LF、VT、FF、NEL、Unicode 行分隔符和段落分隔符现在使用同一套段首判断。
- EPUB 的 `<br>` 与 `div`、`section`、`article`、`stanza` 等正文容器统一转换为明确段落边界，避免部分段落粘连或遗漏缩进；HTML/HTM/XHTML、Markdown、FB2、RTF、DOCX 与在线书源同步进入同一套首行缩进管线。
- 段首已有的普通空格、NBSP、Unicode 空格、全角空格和 BOM 会先规范化，再按阅读设置生成一致的视觉缩进；书签、进度和高亮仍使用原文 canonical offset。

### 开放书源协议与分类浏览

- Open Reading Source Protocol 升级到 `1.4`，书源必须声明 `search`、`detail`、`catalog`、`content` 四项核心阅读能力；协议文档、OpenAPI、示例书源和应用内说明保持一致。
- 书源分类由同步铺开的全部标签改为可搜索的惰性选择器；移动端使用底部面板，宽屏使用对话框，并按书源分组，分类数量较多时不再一次构建所有选项。
- 已添加书源支持手动刷新发现清单，在保留本地启用状态和添加时间的同时更新名称、描述、能力与服务地址；清单不得静默改变书源稳定 ID。

### 应用内更新日志

- 应用内版本历史改为从统一 JSON 资产异步加载，四语内容、版本顺序和回退规则集中维护；页面增加加载失败提示和重试操作。

## v2.2.6 - 2026-07-22

Android 版本号：`2.2.6`，基础构建号：`14137`。

### 下载任务与后台进度

- 在线书籍下载改为共享任务队列，关闭进度弹窗或离开当前页面后仍会继续；书库新增下载任务入口，可统一查看排队、进度、完成与失败状态。
- Android 的书籍和应用更新下载会通过前台服务显示系统进度通知；完成通知可直接打开书籍或进入已校验 APK 的安装流程，通知权限或通知桥失败不会中断下载。

### 在线阅读修复与流畅度

- 修复在线阅读部分段落首行缩进失效的问题：书源 HTML 正文若仅以裸换行分隔段落（未使用 `<p>`/`<div>`/`<br>`），且中间穿插了图片等内联标签，此前换行会被段内空白折叠规则一并吞掉，导致这些段落被拼接成一整段、只有开头能获得首行缩进；现在裸换行统一按段落边界处理，同一章节内的所有段落缩进表现一致。
- 优化在线书源的水平跨章切换：优先预取下一章并复用已预热分页布局，动画结束后直接衔接目标页，避免停稳后出现可见的页面控制器重置。

### Android 图标

- 进一步缩小 Android 普通图标与自适应图标中的书本主体，保持原有品牌图案和配色，同时增加四周留白，改善圆角方形与圆形启动器中的视觉比例。

### 字体资源改为在线下载

- App 界面字体与阅读正文字体不再随安装包一起分发，改为首次使用时按需从 GitHub 在线下载，安装包体积明显减小；已下载字体会保留在本机，删除下载后下次使用会自动重新获取。

### AI 阅读设置与错误提示

- 完善 AI 阅读设置：支持配置多个模型、预设模型快速选择、自动获取可用模型列表，并可分别设置 Base URL、API Key 和 Temperature 等参数。
- AI 服务、语音朗读、书注翻译、阅读计划等模块的失败提示统一改为本地化文案，替代原始异常信息。

## v2.2.5 - 2026-07-20

Android 版本号：`2.2.5`，基础构建号：`14134`。

### 文字阅读内核统一

- 本地 TXT/EPUB 文本与在线书源章节统一使用同一套 canonical text、显示投影、分页页面模型、正文尺寸计算和最终 `RichText` 渲染，避免相同设置在不同入口产生不同换行与分页。
- 在线章节标题改为与结构化 TXT 一致的独占标题页；书源 HTML/纯文本适配器不再预先注入全角缩进或破坏纯文本空行，首行缩进和段间距统一由阅读设置生成。
- 修复部分在线 TXT 章节在独占标题页后，正文第一页仍重复显示同一标题的问题；仅移除正文开头与接口标题或目录标题完全匹配的首行/首段，不影响正文中正常出现的标题文字。

### 在线阅读流畅度

- 修复水平滑动到下一章时在动画过半就重建 `PageView` 导致的顿挫；现在先使用预取和预热完成的真实目标页结束整段横滑，停稳后才提交章节状态，中途回滑不会误切章。
- 下一章内容、分页布局和相邻页预览继续提前准备，跨章提交不等待阅读进度持久化。

### 应用图标

- 更新 Android、iOS、Windows、macOS、Linux、Web 与 OpenHarmony 图标资源，统一各平台的正式品牌视觉和安全区表现。
- 收窄手机首页悬浮导航栏的水平尺寸，增加两侧呼吸空间，同时保持图标、文字模式和点击区域清晰可用。

## v2.2.4 - 2026-07-20

Android 版本号：`2.2.4`，基础构建号：`14133`。

### 开放书源协议 1.3

- App 支持的 Open Reading Source Protocol 版本统一为 `1.3`，协议说明、OpenAPI、示例发现文档、参考书源和应用内说明保持一致。
- 章节目录支持按 `page`、`pageSize` 和 `hasMore` 完整翻页拉取，避免长篇书籍只显示首批章节。
- 客户端遵守书源声明的 `maxCatalogPageSize`；未声明时使用协议默认值，并继续兼容旧书源一次返回完整 `items` 的响应。

### 在线阅读与界面修复

- 修复书源 HTML 正文以 `<br>` 分隔段落时丢失段落边界的问题。
- 缩小手机首页悬浮导航栏高度，减少对内容区域的占用。

## v2.2.3 - 2026-07-20

Android 版本号：`2.2.3`，基础构建号：`14132`。

### 阅读器目录性能

- 修复阅读器目录/书签面板首次打开卡顿、搜索输入及键盘弹出收回时掉帧明显的问题。
- 章节树与搜索过滤结果改为按需缓存，仅在章节数据、搜索关键词或章节展开／折叠状态变化时才重新计算。
- 修复目录/书签弹窗的章节列表在 `StatefulBuilder` 构建闭包内每帧重新分配的问题：该闭包会在键盘弹出/收回动画的每一帧被重新调用，此前每帧都会为全部章节重新创建对象；改为仅在打开面板时构建一次，章节数越多的书受益越明显。
- 修复阅读页在目录搜索框弹出键盘时整页重新排版的问题：阅读页 `Scaffold` 默认会为任意来源的键盘让位（含弹窗内输入框触发的键盘），导致排版区域尺寸在键盘动画的每一帧都发生变化，从而触发当前章节的整页重新分页；阅读页本身没有输入框，现在关闭该让位行为。

### EPUB 排版修复

- 修复部分 EPUB 首行缩进无效／缩进出现在段落中间的问题：源文件中 `<p>`、标题、列表项等段落文本若在 XHTML 里被折行排版，其中的裸换行此前会被误判为新段落，导致缩进插在折行处而非每段真正的开头；现在统一将段落内部空白（含换行）折叠为空格，仅 `<pre>` 保留原始换行。

### Android 图标

- 缩小 Android 自适应图标前景中的书本图形占比（约 80%，四周留出安全边距），避免图标被系统蒙版裁切成圆形/圆角方形等形状时显得比其他 App 图标更大更满；背景渐变保持通栏铺满，边缘无缝衔接。

## v2.2.1 - 2026-07-19

Android 版本号：`2.2.1`，基础构建号：`12120`。

### 平板双页仿真翻页

- 修复右页向左翻动时把当前页镜像当作纸背的问题；例如 8/9 向前翻时，活动纸张正面为 9、纸背为正常方向的 10、底页为 11。
- 本地文件与在线书源阅读器统一使用独立纸背纹理，并覆盖向前、向后、同章、跨章和章节末尾空白补页；手机单页继续保持原有仿真翻页逻辑。
- 新增左右装订边的像素级 Shader 回归，验证纸背页内容与阅读方向。

## v2.2.0 - 2026-07-19

Android 版本号：`2.2.0`，基础构建号：`12119`。

### 更新与官网分发

- 更新检查同时查询 GitHub Releases 与官方站点；发现新版本后可明确选择从 GitHub 更新，或从官网下载。
- Android 官网更新会在应用内按设备 ABI 下载对应 APK，只接受官网 HTTPS 地址及同域重定向，并以 512 MiB 为硬上限在下载过程中即时拦截超出声明大小的响应；下载完成后校验 SHA-256，并在原生层复核包名、实际版本码及当前应用签名后交给系统安装器。Android 8 及以上会按需请求“安装未知应用”授权，最终安装仍由用户确认。
- iOS 当前从官网选项打开网页，待正式上架后再切换 App Store；Windows、Linux 与其他平台继续打开外部下载页。
- GitHub Tag 发布成功后会把 Android、Windows、Linux 安装包和校验清单自动镜像到 `open.xxread.top`；导入前复核固定仓库与 Tag、完整资产集合、校验和、APK 包名、实际版本码和正式签名，官网 stable latest 按平台与架构保持版本单调。
- 官网后台记录真实下载短链的版本、架构、时间、IP 和 User-Agent，用于下载次数、独立 IP、安全防护与故障排查；原始 IP 下载明细最多保留 30 天，生产凭据、数据库、安装包和访问日志均不进入 Git。

### 平板双页阅读

- 阅读设置新增仅平板可见的“双页布局”开关，手机端不显示该选项；关闭后恢复单页并保持当前阅读位置。
- 平板横屏双页时，左页左上只显示章节标题，右页右上只显示时间和电量，避免状态信息重复。
- 修复仿真翻页的跨书脊层级：翻到下一页时右页正确覆盖左页，翻到上一页时左页正确覆盖右页；空白与补位页不再显示虚假页码。

### 仿真翻页与阅读性能

- 重做上一页与下一页的手势跟随、折线几何和回弹收尾，减少纸张脱手、中部起手闪跳、右下甩尾和终点阴影复现。
- 优化平板书脊、中缝和空白补页，本地文件与在线书源使用一致的双页步进、跨章和进度保存逻辑。
- 本地 TXT 阅读页延后到封面转场完成后再初始化，降低打开书籍时的卡顿。
- 在线书源优化相邻章节预取、后台进度保存和分页复用，跨章翻页不再等待较远章节或持久化完成。

### 界面与个性化

- 手机底部悬浮导航支持“纯图标”和“图标＋文字”两种模式，并优化选中状态、尺寸、标签动画与按压反馈。
- 预设与自定义阅读主题可以统一拖拽排序并自动保存；修复主题卡片选中描边被背景遮挡或断裂的问题。
- 阅读统计详情页升级为统一的书卷风格，重新设计总览、图表、热力图、书籍排行和成就页面。
- 时间、章节标题、电量和页码继续作为纸页的一部分，随横向滑动和仿真翻页一起移动。

### 工程结构与可维护性

- `lib/pages` 按首页、书库、书源、阅读器、阅读统计、设置和协议七个功能域重新归档，页面、控制器、主题、组件和私有 `parts` 就近维护。
- 页面文件统一采用 `*_page.dart`、`*_controller.dart`、`*_widgets.dart` 和 `*_part.dart` 命名规则，跨功能域引用统一使用包路径，公开页面 API 保持兼容。

### 第三方书源与项目支持

- 官方发行版明确不预装、不内置、不推荐任何第三方书源；欢迎页和添加书源流程新增独立责任确认。
- 新增 `SOURCE_POLICY.md`，说明项目、独立书源运营者和用户各自的责任边界。
- 设置页新增开发者产品与完全自愿的支持入口；新用户完成欢迎页后会显示一次性项目支持引导。

### 升级提示

- Android 正式发布基础构建号为 `12119`，arm64-v8a split APK 版本码为 `14119`，可覆盖现有 `2.0.5 (14118)` 及更早版本并保留书架、设置、主题和阅读进度。
- 本次更新调整了第三方书源责任说明和官网更新隐私披露，已有用户启动后需要重新确认。

## v2.0.3 - 2026-07-19

Android 版本号：`2.0.3`，基础构建号：`12113`。

### 开发者产品与支持

- 设置页新增“小元读书”和“小元读书社区”推广入口，可直接访问对应官方网站。
- 新增“支持开发”卡片与微信、支付宝两种捐赠二维码弹窗；捐赠完全自愿，不影响任何功能，也不构成购买或服务承诺。
- README 同步增加开发者产品入口和自愿捐赠说明。

### 升级提示

- Android 正式构建基础版本码为 `12113`，arm64 split APK 版本码为 `14113`，可覆盖当前真机上的 `14112` 并保留应用数据。

## v2.0.2 - 2026-07-19

Android 版本号：`2.0.2`，基础构建号：`12110`。

### 阅读纸页信息

- 阅读信息栏重新嵌入无动画、横向滑动和平板双页/经典折页的每张纸页，时间、章节标题和电量会随页面一起滑动或卷曲，不再固定悬浮在屏幕上。
- 上下翻页继续使用固定视口信息栏，保持纵向连续阅读时的稳定位置。
- 手机单页与平板左右页的章内页码向屏幕内侧至少留出 24px，避免被圆角屏幕遮挡。
- 本地文件与在线书源共用同一套纸页信息栏组件、快照更新逻辑和页码安全边距。

### 升级提示

- Android 正式发布基础构建号为 `12110`，可覆盖安装 `2.0.1` 并保留书架、设置、主题与阅读进度。

## v2.0.1 - 2026-07-19

Android 版本号：`2.0.1`，基础构建号：`12109`。

### 仿真翻页

- 优化上一页的经典折页：从屏幕中间向右拖动时立即跟随手指，不再等待边缘追赶动画。
- 上一页折线按手指横向位置推进，纵向来回晃动不会再让纸张脱手或带偏装订边。
- 当前页抓图完成后同时预热前后相邻页，移除上一页快照的额外延迟，降低首次反向翻页的卡顿感。
- 修正反向翻页时物理左装订边的坐标映射，纸张折线与书脊保持稳定贴合。

### 发现与书源

- 发现页的推荐、分类和最新栏目支持“全部书源 / 单一书源”筛选，并复用已加载内容快速切换。
- 多书源最新书籍保留各源内部顺序后均衡穿插，避免单一书源占满首屏。

### 升级提示

- Android 正式发布基础构建号为 `12109`，可覆盖安装现有正式版本并保留书架、设置、主题与阅读进度。

## v2.0.0 - 2026-07-19

Android 版本号：`2.0.0`，基础构建号：`12108`。

### 阅读体验

- 阅读页顶部信息升级为系统状态栏、阅读信息栏和完全沉浸三种模式；分页纸页只保留章内页码，平板双页分别显示在左右外侧。
- 仿真翻页精简为唯一的经典折页，移除样式选择与圆柱卷页实现；继续优化跟手、回弹、装订边、快速释放和连续翻页队列。
- EPUB 段落换行、图片页分页和上下翻页窗口进一步统一，减少异常空白并保持 canonical 阅读位置稳定。

### 自定义主题与导航

- 单一自定义阅读主题升级为可新增、编辑、删除和拖拽排序的多主题库。
- 每套主题支持名称、三种阅读颜色、JPG/PNG/WebP 背景图片以及背景显示强度；旧单主题数据自动迁移。
- EPUB 多级目录支持折叠、搜索祖先路径和自动展开当前章节父链。

### 系统能力

- Android“阅读时保持屏幕常亮”接入真实窗口能力，进入阅读页按偏好启用，退出最后一个阅读页后释放。
- 本地阅读器与在线书源阅读器继续共享主题、顶部信息、安全区、排版和阅读设置。

### 发现与书源

- 发现页默认聚合全部可用书源，并可在推荐、分类和最新栏目按单一书源筛选。
- 最新列表保留各书源内部顺序后均衡穿插，限制单源首批数量，避免某一来源占满页面。
- 多书源内容始终保留来源归属；一个书源加载失败不会丢弃其他来源已成功返回的内容。

### 升级提示

- Android 正式发布基础构建号为 `12108`，可覆盖安装现有正式版本并保留书架、设置、主题与阅读进度。

## v1.2.4 - 2026-07-18

Android 版本号：`1.2.4`，构建号：`12106`。

### 纸页化阅读体验

- 页码、章节名、时间和电量进入每张 page leaf，横滑、无动画和仿真翻页时会与正文作为一张纸共同移动。
- 仿真翻页保留圆柱卷页，并新增经典折页；两种效果共用重新实现的手势几何、速度判定和真实弹簧回弹。
- 经典折页按起手高度连续跟随手指：水平左滑不再被强制吸到右下角，只有真实斜拉时才形成对角折线。
- 快照缓存按页面身份、排版指纹和主题复用，限制 DPR 与字节预算，并按 current、forward、backward 分阶段预热。
- 阅读设置新增首行缩进、段落间距和仿真样式；现有分页器与 canonical 阅读位置保持不变。

### 升级提示

- Android 正式发布构建号为 `12106`，可覆盖安装现有正式版本并保留书架、设置与阅读进度。

## v1.2.2 - 2026-07-18

Android 版本号：`1.2.2`，构建号：`12104`。

### 在线阅读热修复

- 修复书源在线阅读在整书连续滚动模式下，点击屏幕中间无法呼出阅读控制栏的问题。
- 中间轻点恢复显示/隐藏控制栏；上下拖动仍只负责滚动，长按文字选择不会误触控制栏。

### 升级提示

- 本版本 Android 正式发布构建号为 `12104`，可覆盖安装 1.2.1 并保留书架、设置与阅读进度。

## v1.2.1 - 2026-07-18

Android 版本号：`1.2.1`，构建号：`12103`。

### 阅读器统一补全

- 在线书源阅读的“上下滚动”模式新增“按章节滚动”开关，并与本地阅读共用同一个持久化设置。
- 开启时保持单章上下滚动、左右滑动切换章节；关闭后可将在线章节懒加载为整本书连续纵向内容流。
- 整书连续滚动会随当前可见内容同步章节位置、目录状态、书签上下文和章内阅读进度。

### 排版修复

- 修复中文正文软换行后左右留白不对称的问题，本地阅读与在线书源正文统一使用两端对齐。
- 分页测量和最终绘制使用同一文字流规则，滚动、无动画、水平滑动和仿真翻页模式表现一致。

### 升级提示

- 本版本 Android 正式发布构建号为 `12103`，可覆盖安装现有正式版本并保留书架、设置与阅读进度。

## v1.2.0 - 2026-07-18

Android 版本号：`1.2.0`，构建号：`10101`。

### 阅读体验

- 本地阅读与在线书源阅读共用同一套设置面板、控制栏、翻页模式和安全区计算，体验一致。
- 支持独立的上、下、左、右边距；水平边距可调至 0，正文在可用宽度内居中，左右留白对称。
- 裁剪首行上方与末行下方的行距 leading，上下边距为 0 时正文更贴合可用区域，同页可显示更多文字。
- Android 音量键翻页正式接入：音量减下一页、音量加上一页；无动画、水平滑动、仿真翻页可用，上下滚动模式仍保持系统调音量。

### 视觉与界面

- 美化状态栏与阅读控制栏，统一玻璃质感样式。
- 重做书籍打开与关闭动画。
- 优化平板书库布局。

### 字体与封面

- 完善自定义字体：TTF/OTF 导入后可同时用于 App 界面与阅读正文，支持管理、去重与安全回退。
- 无封面书籍统一生成简约封面，本地导入与书源加入书架/下载行为一致；真实封面始终优先。

### 导入与许可

- 重构书籍导入确认页：选中后更专注确认，主操作按钮适配底部安全区，避免被系统栏遮挡。
- 设置页“关于”新增开源许可入口，可离线查看 AGPL-3.0、历史 MIT 与内置字体许可。

### 书源

- 项目聚焦 Open Reading Source Protocol（ORSP 1.1），移除旧版阅读书源兼容实现。

### 升级提示

- 本版本 Android 正式发布构建号为 `10101`；后续版本必须使用更高构建号才能覆盖安装。

## v1.1.0 - 2026-07-17

Android 版本号：`1.1.0`，构建号：`10000`。

- 新增自定义字体。
- 新增加入书签。

## v1.0.2

Android 正式发布构建号为 `6009`。

### 发现页

- 发现页专注书籍展示，移除页首介绍卡片和内嵌搜索面板。
- 推荐、分类和最新栏目分别缓存，切换栏目不再重复请求，并支持下拉刷新。
- 进入分类栏目后自动选中第一个分类并展示书籍。
- 书源增删或启停后自动刷新发现内容。

### 独立搜索页

- 搜索入口移动到发现页右上角，点击后进入全屏搜索并自动聚焦输入框。
- 支持聚合搜索全部已启用书源，或只搜索指定书源。
- 支持一键清除、滚动加载更多和失败重试。
- 书籍详情不再展示书源 ID、书籍 ID 等调试信息。

### 书架

- 书架搜索改为顶栏展开式搜索。
- “全部 / 在读 / 已读”筛选收纳到顶栏菜单。
- 修复玻璃效果下书架顶栏缺少毛玻璃质感的问题。

### 阅读设置

- 修复阅读设置面板无法通过顶部拖动区域下拉关闭的问题。
- 统一本地阅读器和在线书源阅读器的设置入口、翻页模式与字号范围。
- 字号、行距和边距在结束拖动后再应用排版，减少拖动卡顿。

## v1.0.1

### 发现与书源

- 新增发现体验，支持书源推荐、分类和最新内容浏览。
- 搜索结果支持自动加载下一页、手动加载更多和失败重试。
- 多书源分页独立维护状态，并在聚合结果时自动去重。

### 开源许可

- 从本版本起采用 GNU AGPL-3.0-only。
- `v1.0.0` 及更早版本已经获得的 MIT 权利继续有效。
- 新增授权边界、贡献许可和项目品牌说明。
- 更新用户协议与多语言许可文案。

## v1.0.0

- 跑通开放书源流程，支持将在线书籍加入书架、在线阅读和下载阅读。
- 新增多套阅读主题。
- 新增仿真翻页效果。
- 新增行间距调整。
- 优化阅读排版算法。

## v0.9.1

- 新增平板双页阅读布局与相关分页能力。
- 完善 Android、iOS、macOS、Windows 和 Linux 的跨平台发布准备。
- 增加 Android Release 自动签名和跨平台 GitHub Release 流程。
- 完善多语言发布内容、用户协议和项目网站信息。
- 改进原生 Flutter 阅读器的分页、排版、主题和安全保护。
